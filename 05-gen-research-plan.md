# Dev A — 생성 엔진 연구 계획서 (v1)

> `01-dev-a-generation.md` §2(베이크오프)·§4(프롬프트)의 **실행 계획서이자 결정 기록부**. 충돌 시 00/01이 우선한다. 인터페이스 계약(00-common §5)은 이 연구로 변하지 않는다.
> 실행 도구의 스펙은 `06-gen-research-tools-spec.md` 참조 — 두 문서는 짝이다.

---

## 0. 목적 · 독자 · 관련 문서

- **목적**: "사람 사진 + 상품 레퍼런스 + wear_position → MCM 화보" 문제를 풀 모델·프롬프트·운영 정책을 **3~4일 안에** 데이터로 확정한다.
- **독자**: Dev A(실행 주체). Dev B는 §8의 "00-common §7 기입 시점"만 보면 된다(블로킹 해제 신호).
- **관련**: 00-common §2(v1 정책)·§7(기입 대상)·§9(페르소나), 01-dev-a §0(우선순위)·§2(프로토콜 원본)·§4(프롬프트), 06(도구 스펙).
- **전제(확정)**: 사용 가능 API = **OpenAI + Google AI 2종**. fal.ai/Replicate 키 미보유 → FLUX Kontext/Seedream 계열은 §3 R1의 **컨틴전시 후보**로만 유지.

---

## 1. 목표와 산출물 (우선순위순)

| # | 산출물 | 확정 시점 |
|---|---|---|
| 1 | **00-common §7.1(상품컷 스펙)·§7.2(사용자 사진 스펙) 기입** = Dev B 블로킹 해제 | R2 종료 즉시 |
| 2 | 승자 모델 1 + 백업 1 + **Go / No-Go** 판정 | R2 종료 |
| 3 | 스카프(neck) 포함 여부 — 공통 문서 미결② 해소 | R2 종료 |
| 4 | 확정 프롬프트 **pv2** + N/재시도/타임아웃 정책 = 실엔진 구현 스펙 | R3~R4 종료 |
| 5 | 데모 자산 p1~p3 납품 (`apps/web/public/demo/`) | R6 종료 |

**원칙: R2가 슬립하면 R3·R4를 줄여서라도 산출물 1(§7 기입)을 먼저 낸다.** B의 대량 이미지 수집이 여기에 걸려 있다.

---

## 2. 실험 설계

### 변수 (독립)

| 축 | 값 |
|---|---|
| 모델 | `gemini_nb`(Google 이미지 편집 계열), `gpt_image`(OpenAI) — 컨틴전시: `flux_kontext`(fal.ai, R1 전멸 시에만) |
| 셀카 | 3장 — 포멀도 축 분리: casual / smart_daily / formal (01 §2). 동의된 실인물, p2 대비 남성 1장 포함 권장 |
| 상품 | 3 SKU — MMRGATA04CO001(비세토스, cross) / MMPGADC01BK001(무지 카프스킨+골드 플레이트, shoulder) / MMKGATA03MT001(나일론 백팩, back) + 스카프 MEFGAMM12CO001(neck) 1케이스 |
| 프롬프트 | pv1(R2 동결) → pv2(R3 확정). 버전은 추가만, 수정 금지 |
| 반복 | R1·R2 **N=2**(운영 정책 N=2와 동형 — 결과가 곧 운영 예측) / R4 분산 측정 N=6 / R6 N=6~8 |

### 통제 조건

- 동일 셀카 파일·동일 상품컷·단일 pv(R2 중 동결)·단일 채점자(Dev A).
- 출력은 3:4 세로 목표. 프로바이더가 미지원하면 최근접 비율 사용하고 manifest에 기록.
- 프로바이더별 API 문법 차이는 어댑터가 흡수하되 **지시의 의미는 불변**. 이탈 시 manifest에 기록.

### 셀 정의

셀 = (셀카 × 상품). 식별자 `{model}__{selfie}__{sku}__{pv}__r{n}` (파일명 겸용). R2 본 매트릭스 = 2모델 × 9셀 × N2 = 36장 + 스카프 2모델 × 1셀 × N2 = 4장.

---

## 3. 단계별 계획 R0~R6

### 요약표

| 단계 | 목적 | 종료 기준(게이트) | 타임박스 | 장수 | 예상 비용 |
|---|---|---|---|---|---|
| R0 준비 | 키 2종·자산·툴 코어 | fake로 run→gallery→report 왕복 + 각 키 1콜 검증 | 4~5h | 0~2 | ~$0 |
| R1 스모크 | 프로바이더 생존성 | **G1** 진행 모델 확정 | 1~2h | 4 | <$1 |
| R2 본 베이크오프 | 승자·백업·Go/No-Go·스카프·§7 | **G2(최우선)** 산출물 1~3 확정 + 00 §7 기입 커밋 + B 통지 | 3~5h | ~40 | $2~5 |
| R3 프롬프트 심화 | pv2 확정 | 축별 결론 + pv2 동결 | 2~3h | 32~48 | $2~7 |
| R4 안정화 | 분산·엣지·실패 분류 | **G3** N·재시도·타임아웃 배분 동결 | 1.5~2h | ~24 | $1~4 |
| R5 실엔진 이관 | 승격·배포 | **G4** Railway ENGINE 교체 E2E + 롤백 검증 + B 통지 | 2~3h | ~4 | <$1 |
| R6 데모 자산 | p1~p3 납품 | 페르소나별 전항목 pass 1장 → public/demo 커밋 | 2~3h | 24~32 | $1~5 |

총 ~130~150장 / **$5~20 예상** (하드 가드 $50, §6). 3~4일 배치: **D1** = R0+R1(+툴 빌드 B1~B4) / **D2** = R2 → §7 기입·B 통지 → R3 착수 / **D3** = R3 마무리 + R4 + R5 / **D4** = R6 + 버퍼.

### R0 준비 — 체크리스트

- [ ] OpenAI·Google AI 키 결제 확인, 각 1콜 스모크, **이미지 편집 모델 ID 최신 확인 → `models.yaml` 기입** (모델명은 코드가 아니라 설정 문자열 — 06 §3)
- [ ] 상품컷: 베이크오프 3종 + **스카프(MEFGAMM12CO001) 공식몰 컷 직접 저장** (B 대기 금지 — 01 §2 관례), `prep_assets`로 정규화
- [ ] 셀카 3장 확보(동의 확인) → `assets/selfies/`, 레지스트리 기입
- [ ] 툴 B1~B3 완성, fake 프로바이더로 $0 E2E (06 §12)
- [ ] 루트 `.gitignore`에 `research/assets/`·`research/experiments/` 추가 (실인물·공식몰 이미지 미추적)

### R1 스모크 — 생존성 판정 (1셀카 × 1상품 × 2모델 × N2 = 4장)

입력 권장: 셀카 smart_daily × 상품 No.09(비세토스 — 가장 어려운 패턴 재현). 생존 기준: **정책 거부 없이 1장 이상 유의미한 합성 산출**. 생존 판정과 별개로 6가지 관찰을 기록한다:

1. **실인물 얼굴 편집에 대한 정책 거부 여부 (최우선)** — 프로바이더별로 상이하며, 여기서 후보가 통째로 탈락할 수 있다
2. 로고·상표 재현 거부 또는 임의 변형
3. 다중 이미지 입력 지원 방식(장수 제한·전달 순서)
4. 지원 출력 사이즈·종횡비(3:4 가능한가)
5. 가시 워터마크 유무
6. 대략적 지연

**G1 판정**: 2종 생존 → R2를 2모델로 / 1종 생존 → 단독 진행 + "백업 부재" 리스크 기록, 컨틴전시 후보 1종 병행 검토 / **0종 생존 → 컨틴전시 발동: fal.ai 키 발급 + FLUX Kontext 어댑터 추가(httpx REST, SDK 불요 — 06 §4) 후 R1 재실행**. 컨틴전시까지 전멸이면 §5의 전멸 시나리오.

### R2 본 베이크오프

1. `run configs/r2_bakeoff.yaml` (동시성으로 벽시계 30~60분) + `r2b_scarf.yaml`
2. 갤러리 채점 45~60분 (§4 프로토콜)
3. `report` → 순위 제안 확인 → **승자·백업·Go/No-Go·스카프 판정** (§5 규칙)
4. 부록 B 템플릿으로 **00-common §7.1/§7.2 기입 + 미결② 해소 + 본 문서 부록 A 기록 — 같은 커밋**, B에게 통지

### R3 프롬프트 심화 — 승자 모델만, 축별 독립 1:1 비교 (full factorial 금지)

| 축 | 비교 |
|---|---|
| ① ref 이미지 장수·순서 | 1장 vs 2~3장 |
| ② 부정 지시 블록 | 유 vs 무 (로고 변형·신체 변형 금지문) |
| ③ 프롬프트 언어 | pv1(영어 템플릿+한국어 DB 원문 임베드) vs 전량 영어 번역 |
| ④ style_hints | lighting만 vs 미사용 — city 무드는 v1 톤보정 정책상 최소 활용(밀라노=스트리트 등 v2 정의 주의) |
| (⑤ 승자가 gpt 계열일 때만) 품질 티어 | mid vs high (단가 ~3배 — R2는 mid 통일) |

축당 대표 셀 2~3개 × N2. 산출: **pv2 동결** + 축별 결론 한 줄(부록 A).

### R4 안정화

- **분산**: 최고 셀·경계 셀 2개를 N6 반복 → 전항목 pass율 p 추정 → **라이브 N=2 성공률 = 1-(1-p)²** 산출 (이 숫자가 데모 리스크의 실측치)
- **back 위치 엣지 심화**: 정면 셀카에서 스트랩 표현 관찰 — 실패 경향이면 §7.2에 "백팩은 반측면 이상 권장" 반영 + back 전용 지시 보강
- **실패 유형 분류**: refused / fake-product / distortion / position-ignored / timeout / network → 유형별 엔진 대응 확정: network·timeout → 재시도 1회 / refused → 프롬프트 순화 변형 1회 / 품질 실패 → **N=2 병렬 first-ok** (v1에 자동 품질 판정이 없으므로 "N=2 생성 후 선택"(01 §4)의 현실화. VLM judge 선별은 P1)
- **타임아웃 배분 동결**: 다운로드 5s + 생성 40s + 업로드 5s + 여유 5s = 55s (`engines/base.py`의 `default_timeout_seconds=55.0`과 정합)

### R5 실엔진 이관 — 06 §11 절차

복사-이식 → `_BUILDERS` lazy 등록 → requirements 최소 추가 → Railway env(`ENGINE={winner}` + 승자 키 + `SUPABASE_*`) → curl 스위트 + 실생성 E2E 1건 → **롤백 리허설(`ENGINE=mock` 원복만으로 복구되는지)**. 덤: `services/gen/static/samples/`를 베이크오프 합격작으로 교체(mock 폴백 화면도 리얼해짐).

### R6 데모 자산 — 러너 재사용

`r6_personas.yaml`(모델=승자, 셀=p1~p3 × 확정 SKU, N6~8, pv2) → 갤러리에서 **6항목 전부 pass 1장씩 선별** → `prep_assets`로 3:4 정리 → `apps/web/public/demo/{p1..p3}/photo.jpg·result.jpg·meta.json`(태그 3종·SKU·추천 이유 1줄) 커밋. p4~p6은 미결③(도시 데이터) 도착 시 config 추가만으로 재실행.

---

## 4. 평가 프로토콜

체크리스트 6항목 = 01 §2 원문에 안정 ID 부여. **항목별 fail 기준** — 하나라도 해당하면 fail:

| ID | 항목 | fail 기준 |
|---|---|---|
| c1 | 패턴·로고·하드웨어 톤 일치 | 비세토스 반복 단위 왜곡 / 로고 플레이트 문자 뭉개짐 / 골드·실버 뒤바뀜 / 컬러(꼬냑 등) 이탈 |
| c2 | 크기·착용 비율 타당 | DB 사이즈 대비 명백한 과대·과소 (미디엄 크로스바디가 토트 크기 등) |
| c3 | 지정 wear_position 합성 | 지정 위치가 아닌 곳에 합성 / back인데 스트랩 표현조차 없음 |
| c4 | 인물 보존 | 얼굴 왜곡·동일성 훼손 / 손가락 왜곡 / 의상 임의 변형 |
| c5 | 조명·그림자 융합 + 원본 배경 유지 | 배경 변형 / 그림자 없음·방향 불일치로 제품이 "붙인 티" |
| c6 | 화보 톤 | 광고 화보로 못 쓸 어색함 (주관 — 보수 판정) |

**채점 규칙**: 이미지(rep) 단위 채점 / 단일 채점자(Dev A — 일관성) / **애매하면 fail** / note 필드에 실패 사유 한 줄 의무(→ §7 스펙·R4 실패 분류의 원천 데이터). **셀 통과 = N개 중 ≥1장이 6항목 전부 pass** (best-of-N — 운영 N-best와 동형). 도구(report)는 순위를 "제안"만 하고, 확정은 A가 갤러리 재확인 후. 승자 확정 전 B의 30분 스팟체크 권장(결정권은 A).

---

## 5. 승자 결정 규칙 · Go/No-Go · 스카프

### 부적격 게이트 (순위 무관 승자 불가)

- 거부+오류율 > 20% (R2 20회 중 4회 초과)
- 단건 생성 p95 > 40s (55s 예산 − I/O ~10s − 여유)
- 제거 불가한 가시 워터마크

### 타이브레이커 사슬 (주 지표 동점 시 순서대로)

**주 지표 = 셀 통과 수(9셀 중)** → ① c1+c2 통과 수(제품 정합성) → ② c4(인물 보존) → ③ c3+c5(위치+융합) → ④ c6(화보 톤) → ⑤ p95 낮은 쪽 → ⑥ 단가 낮은 쪽 → ⑦ 운영성 판단 한 줄 기록. (01 §0 품질 우선순위와 1:1 — 자의성 제거, 결정 재현 가능.)

**백업 모델** = 순위 2위. 2모델 구도에서는 자연히 타 벤더가 되어 단일 장애점을 회피한다(발표 당일 한 벤더 장애 대비).

### Go / No-Go (기본값 — 판정 시 조정 가능하나 조정 사유를 부록 A에 기록)

| 판정 | 조건 | 행동 |
|---|---|---|
| **GO** | 승자 셀 통과 ≥ 6/9 AND c1 통과율 ≥ 70% AND 부적격 게이트 통과 | R3~R6 전체 진행 |
| **CONDITIONAL GO** | 셀 통과 4~5/9 | 제한 GO — 약한 wear_position 제외, §7.2 사진 조건 강화, 제한 내용을 데모 시나리오에 반영 |
| **NO-GO** | 셀 통과 < 4/9 또는 "그럴듯한 가짜 제품"이 구조적 | 라이브 생성 격하(`ENGINE=mock` 유지), 사전 생성 중심. **R6는 최선 모델로 계속**(대량 뽑기+선별은 여전히 성립). B의 계약·플로우 불변 — B를 끌어들이지 않는다 |
| (전멸) | 컨틴전시 포함 전 모델 정책 거부 | 데모 자산 자체가 위험 → 스코프 축소 피벗(A 주도) |

### 스카프 판정 (미결② 해소)

승자 모델 출력 중 ≥1장이 **c1·c3(자연스러운 목 배치)·c4·c5 동시 pass**면 포함. **애매하면 제외 — 디폴트 편향 = 제외.** '제외'도 미결②의 유효한 해소이며, 00-common §8 neck 주석과 B의 matchable 플래그에 반영을 통지한다.

---

## 6. 비용 · 볼륨 계획

- 단가 가정(전부 `models.yaml`의 `cost_per_image_usd`로 조정 가능한 추정치): Gemini 이미지 편집 ~$0.04 / gpt-image mid ~$0.07, high ~$0.19 / (컨틴전시) FLUX Kontext ~$0.04~0.08
- 총 ~130~150장, **$5~20 예상. 하드 가드 $50** — 초과 시 즉시 중단하고 계획 재검토
- 절차적 안전장치: 모든 실행 전 `--dry-run` 견적 확인 의무 + config별 `budget_cap_usd`(초과 시 러너가 실행 거부)

---

## 7. 리스크 레지스터

| 리스크 | 징후 | 완화 | 트리거 시 행동 |
|---|---|---|---|
| 실인물 얼굴 편집 정책 거부 | R1 refused | R1 조기검증 / "편집" 프레이밍 프롬프트 | G1 컨틴전시 → 그래도 전멸이면 §5 전멸 시나리오 |
| 로고·상표 재현 거부/변형 | c1 연쇄 fail | "레퍼런스의 제품을 그대로"(생성 아닌 편집) 프레이밍 | 백업 모델 전환 |
| 레이트리밋·신규 쿼터 | 429 | R0 티어 확인 / 동시성 per-model 2 / 백오프 / resume | 동시성 하향 재실행 |
| 비용 폭주 | dry-run 견적 초과 | budget_cap + $50 가드 + 단가 레지스트리 | 실행 중단·볼륨 축소 |
| 프로바이더 장애·지연 스파이크 | timeout 다발 | resume / 기록 후 스킵 | 백업 모델로 진행 |
| 출력 종횡비 제약 | 3:4 불가 | R1 기록 | 크롭 후처리 + §7 스펙 반영 |
| 가시 워터마크 | R1 확인 | — | 부적격 게이트 |
| p95 > 40s | R2 지연 통계 | N=2 병렬 | 부적격 게이트 |
| 타임박스 슬립 | D2 내 G2 미달 | 게이트별 디폴트 액션 | 현재까지 최고 모델로 Go, R3 축소 — §7 기입은 불가침 |
| 셀카 개인정보 | — | 동의된 사진만 / gitignore / 전송 전 리사이즈 | — |
| 미결③ 미도착 (도쿄·파리·뮌헨) | — | A 리스크 아님(의존성) — p4~p6만 차단 | 도착 시 R6 config 추가 |

---

## 8. 문서 갱신 의무 ("같은 커밋" 원칙)

| 트리거 | 대상 | 내용 |
|---|---|---|
| **R2 종료 즉시** | 00-common §7.1/§7.2 | 입력 스펙 기입 (부록 B 템플릿 → 확정값) |
| 〃 | 00-common 미결 목록·§8 | 미결② 해소, neck 주석 갱신 |
| 〃 | 본 문서 부록 A | 승자·백업·Go/No-Go·스카프 + report 요약표 붙여넣기 |
| 〃 | Dev B | 블로킹 해제 통지 (대량 이미지 수집 시작 신호) |
| R5 종료 | 00-common §10 env 표 | "이미지 생성 API 키" 변수명 구체화 |
| 〃 | services/gen/README | 현재 엔진 표기 갱신 |

03-mock-service-spec.md(계약 문서)는 불변. 미결①(피칭 문구)은 A 소관 아님.

---

## 9. 안티스코프 (이 연구에서 하지 않는 것)

파인튜닝·LoRA·셀프호스팅(원천 금지) / 배경 교체 실험(P2 스트레치) / 의류(garment) / 멀티 앵글 / 속도 최적화 작업(로딩 연출이 커버) / 통계적 유의성 추구(N=2는 방향타 — 결정은 데이터+판단) / **자동 품질 지표(CLIP·SSIM 등) 일체** — 1순위 기준 "그 로고가 정확히 그 로고인가"는 자동 지표가 가장 못 하는 판별이고, ~150장 × 6항목은 갤러리로 1~2시간 인간 채점이면 끝난다 / 연구 단계 VLM judge(승자 확정 후 엔진 P1 기능으로만) / 계약(00 §5) 변경 일체.

---

## 부록 A. 결정 기록 (실행하며 채움 — 이 표가 유일한 영구 기록)

| 항목 | 결정 | 근거 | 일시 |
|---|---|---|---|
| R1 생존 모델 | **2종 생존** (gemini_nb · gpt_image) | 스모크 4/4 ok, refused 0. 정책 거부 미관측 | 2026-08-15 |
| R2 승자 / 백업 | **승자 gpt_image** (gpt-image-2, quality=mid, 1152×1536) / **백업 gemini_nb** | 아래 §A-1 표 + §A-2 게이트 조정 | 2026-08-16 |
| Go / No-Go | **GO** | 셀 통과 9/9(100%), c1 100%, 부적격 게이트 3개 통과 | 2026-08-16 |
| 스카프 포함 여부 | **포함** | 승자 출력이 c1·c3·c4·c5 동시 pass. 미결② 해소 | 2026-08-16 |
| §7.1 / §7.2 기입값 | 00-common §7.1/§7.2 기입 완료 | §A-3 (R2c 실험) | 2026-08-16 |
| pv2 diff 요약 (축별 결론) | *(R3)* — 축① 일부 선행 확정: §A-3 | | |
| N / 재시도 / 타임아웃 정책 | *(R4)* | | |

### A-1. R2 실행·채점 요약 (report 발췌)

R2 = r2_bakeoff(2모델 × 셀카3 × 상품3 × N2 = 36장) + r2b_scarf(2모델 × 1셀 × N2 = 4장).
실지출 $3.40. **40장 전량 ok — refused 0 · error 0 · timeout 0.**

| 모델 | 시도 | ok | refused | error | timeout | 거부+오류율 | p50 | p95 | Σ비용(추정) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| gpt_image | 18 | 18 | 0 | 0 | 0 | 0.0% | 41.1s | 55.5s | $1.44 |
| gemini_nb | 18 | 18 | 0 | 0 | 0 | 0.0% | 12.4s | 18.4s | $1.62 |

| 모델 | 셀 통과 | 채점 이미지 | c1 | c2 | c3 | c4 | c5 | c6 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| gpt_image | **9/9** | 18 | 100% | 100% | 100% | 100% | 100% | 100% |
| gemini_nb | 0/9 | 0 | — | — | — | — | — | — |

> **⚠ gemini_nb의 0/9는 품질 실패가 아니라 미채점이다.** 스폰서 제약으로 승자가 gpt_image로
> 먼저 확정되어, 백업 모델은 채점 대상에서 제외했다. 실행 통계(거부·오류·지연)는 전량 유효하다.
>
> **⚠ 채점 방식**: 갤러리 육안 확인 후 **구두 일괄 판정**(전 항목 pass)이며, 이미지별 개별
> 체크가 아니다. scores.json은 그 판정을 전사한 것으로 `scorer` 필드에 명시돼 있다.
> fail 0건이므로 §4의 "fail마다 note 한 줄" 의무는 발동하지 않았고, 결과적으로
> **§7 스펙을 *강화*할 관측 근거는 이번 실행에서 나오지 않았다**(§A-3 참조).

### A-2. 부적격 게이트 ② 조정 (§5 기본값 조정 — 사유 기록 의무 이행)

기본값 "단건 생성 p95 > 40s"를 **"N=2 병렬 first-ok 기준 총 소요 > 60s"**로 재유도한다.

- **근거**: 40s는 55s 내부 배분에서 파생된 값이고, 계약상 실제 상한은 00-common §5 폴링
  규약의 **60초**다(B 최대 대기 60s / A 60초 내 done·failed 종결 의무).
- **실측**: gpt_image 단건 p95 55.5s(n=18) → N=2 병렬 first-ok p95 **46.2s**(경험분포).
  I/O 10s 가정 시 총 56.2s로 60초 계약 충족(여유 3.8s). R2c의 같은-조건 재현에서는
  단건 p95 **43.1s**로, 55.5s가 이상치였음이 확인됐다.
- **미검증 잔여**: 다운로드·업로드 실측치가 없다. I/O가 15s를 넘으면 계약 초과율이 11%로
  오른다 → **R5 배포 E2E에서 I/O를 실측해 이 판정을 재확인한다.** 여유가 부족하면
  출력 크기 축소(912×1216, 정확히 3:4·16배수, gemini와 동일 화소)를 카드로 남겨 둔다.
- **부수**: gemini_nb를 백업으로 유지해 벤더 단일 장애점을 회피(§5).
- 게이트 ①(거부+오류율) 0.0%, 게이트 ③(가시 워터마크) 미관측 — 조정 없이 통과.

> **도구 주의**: `report.py`는 §5 **기본값** 40s를 하드코딩하고 있어, 자동 출력은
> gpt_image를 부적격 처리하고 "승자 gemini_nb / NO-GO"라는 잘못된 제안을 낸다.
> `MAX_P95_MS`를 고치지 않은 것은 의도적이다 — 코드를 바꾸면 R3·R4에서 조정된 규칙이
> 조용히 적용되어 §5 원문과 코드가 어긋난 이유를 추적할 수 없게 된다. 조정은 이 기록으로만 산다.

### A-3. R2c — 레퍼런스 장수 축 (R3 축① 선행 확정)

§7.1의 "3/4 각도 1장 권장"을 그대로 확정해도 되는지 검증. 이 문구가 **B의 전 카탈로그
수집 분량**을 정하므로 틀렸을 때의 재작업 비용이 가장 크다.
config `r2c_refcount.yaml` — gpt 단독, `max_ref_images`만 다른 1:1(gpt_ref1/gpt_ref2),
3 SKU × s_casual × N2 = 12장, $0.96, 12/12 ok.

| | ref_count | p50 | p95 | 입력 토큰 |
|---|---:|---:|---:|---:|
| gpt_ref1 (정면만) | 1 | 40.2s | 43.1s | 2683~2852 |
| gpt_ref2 (정면+보조컷) | 2 | 39.1s | 42.8s | 3554~4123 |

**결론 (육안 1:1 대조 — 체크리스트가 아닌 R3 축별 결론 형식):**

1. **제품 동일성이 확실히 개선됐다.** 단 R2가 이미 9/9 전항목 pass라 c1이 천장에 붙어 있어,
   **체크리스트로는 잡히지 않는 합격선 위쪽의 개선**이다. → §7.1을 "필수"가 아니라 **"권장"**으로 확정.
2. **패턴 회귀 없음.** 크로스 보조컷이 578×842로 1024 하한 미달인데 비세토스 패턴이 유지됐다.
   표본이 1 SKU × 2장이라 **하한을 낮추는 근거로는 약하다** — 초안값 1024를 유지하고 관측만 기록.
3. **[음성 결과] 백팩 스트랩 표현은 개선되지 않았다.** 정면 팩샷에 어깨 스트랩이 없으므로
   후면 등판컷을 주면 c3의 back 표현이 좋아지리라 가정했으나 **반증됐다.**
   → **back 스트랩은 레퍼런스 이미지로 해결되지 않는다. 프롬프트 측 문제로 확정하고 R3(배치 지시)·
   R4(back 엣지)로 이관한다.** "레퍼런스를 더 주면 되지 않을까"를 재시도하지 말 것.
4. 각도 종류(사선 3/4·탑다운·후면)별 우위가 관측되지 않아 **§7.1에서 각도를 지정하지 않는다.**
5. 지연 페널티 없음, 비용 +$0.01/장 수준 — 보조컷 추가의 운영 비용은 무시할 만하다.

### A-4. 자산 처리 기록

- `s_smart`를 R2 착수 직전 교체(구 380×1244 → 1440×1800). 교체분이 **확장자만 `.jpg`인 WebP**
  였고 두 어댑터 모두 입력 MIME을 `image/jpeg`로 하드코딩하므로, 그대로 실행했다면 s_smart 행
  16셀이 400 위험에 노출됐다(= 33% error → 게이트 ①을 우리 자산으로 자초). `prep_assets`로
  1229×1536 진짜 JPEG로 정규화 후 실행. WebP 원본은 `assets/_originals/`에 보관.
  `prep_assets --verify`는 확장자만 보므로 이 사고를 잡지 못한다 — 알려진 구멍.
- s_smart는 룩북성 촬영물이다. 실사용자 셀카는 s_casual·s_formal 2장(40장 중 24장 생성분)이며,
  **§7.2가 실사용자 조건 대비 낙관 편향일 수 있다.** R4/R6 착수 전 실사용자 셀카 추가 확보 권장.

## 부록 B. 00-common §7 기입 초안 템플릿 (R2 채점 노트로 값 확정)

**§7.1 상품 레퍼런스 이미지 스펙 (초안)**

| 항목 | 초안값 |
|---|---|
| 배경 | 화이트/뉴트럴 팩샷 |
| 필요 각도/장수 | 정면 1장 필수 + 3/4 각도 1장 권장, 상한 3장 |
| 최소 해상도 | 긴 변 1024px 이상 |
| 금기 | 모델 착용컷 / 워터마크 / 로고 가림 / 배경 소품 |

**§7.2 사용자 사진 스펙 (초안)**

| 항목 | 초안값 |
|---|---|
| 프레이밍 | 무릎 위 이상 (back 상품 매칭 대비 반측면 허용 문구는 R4 결과로) |
| 인원 | 1명 |
| 최소 해상도 | 긴 변 1024px 이상 |
| 조명 권고/금기 | 균일광 권장 / 강한 역광·극단 각도·얼굴 가림 금기 |
