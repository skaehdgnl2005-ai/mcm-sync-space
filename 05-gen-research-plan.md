# Dev A — 생성 엔진 연구 계획서 (v1)

> `01-dev-a-generation.md` §2(베이크오프)·§4(프롬프트)의 **실행 계획서이자 결정 기록부**. 충돌 시 00/01이 우선한다. 인터페이스 계약(00-common §5)은 이 연구로 변하지 않는다.
> 실행 도구의 스펙은 `06-gen-research-tools-spec.md` 참조 — 두 문서는 짝이다.

---

## 0. 목적 · 독자 · 관련 문서

- **목적**: "사람 사진 + 상품 레퍼런스 + wear_position → MCM 화보" 문제를 풀 모델·프롬프트·운영 정책을 **3~4일 안에** 데이터로 확정한다.
- **독자**: Dev A(실행 주체). Dev B는 §8의 "00-common §7 기입 시점"만 보면 된다(블로킹 해제 신호).
- **관련**: 00-common §2(v1 정책)·§7(기입 대상)·§9(페르소나), 01-dev-a §0(우선순위)·§2(프로토콜 원본)·§4(프롬프트), 06(도구 스펙).
- **전제(확정)**: 사용 가능 API = **OpenAI + Google AI 2종**. fal.ai/Replicate 키 미보유 → FLUX Kontext/Seedream 계열은 §3 R1의 **컨틴전시 후보**로만 유지.

---

## 1. 목표와 산출물 (우선순위순)

| # | 산출물 | 확정 시점 |
|---|---|---|
| 1 | **00-common §7.1(상품컷 스펙)·§7.2(사용자 사진 스펙) 기입** = Dev B 블로킹 해제 | R2 종료 즉시 |
| 2 | 승자 모델 1 + 백업 1 + **Go / No-Go** 판정 | R2 종료 |
| 3 | 스카프(neck) 포함 여부 — 공통 문서 미결② 해소 | R2 종료 |
| 4 | 확정 프롬프트 **pv2** + N/재시도/타임아웃 정책 = 실엔진 구현 스펙 | R3~R4 종료 |
| 5 | 데모 자산 p1~p3 납품 (`apps/web/public/demo/`) | R6 종료 |

**원칙: R2가 슬립하면 R3·R4를 줄여서라도 산출물 1(§7 기입)을 먼저 낸다.** B의 대량 이미지 수집이 여기에 걸려 있다.

---

## 2. 실험 설계

### 변수 (독립)

| 축 | 값 |
|---|---|
| 모델 | `gemini_nb`(Google 이미지 편집 계열), `gpt_image`(OpenAI) — 컨틴전시: `flux_kontext`(fal.ai, R1 전멸 시에만) |
| 셀카 | 3장 — 포멀도 축 분리: casual / smart_daily / formal (01 §2). 동의된 실인물, p2 대비 남성 1장 포함 권장 |
| 상품 | 3 SKU — MMRGATA04CO001(비세토스, cross) / MMPGADC01BK001(무지 카프스킨+골드 플레이트, shoulder) / MMKGATA03MT001(나일론 백팩, back) + 스카프 MEFGAMM12CO001(neck) 1케이스 |
| 프롬프트 | pv1(R2 동결) → pv2(R3 확정). 버전은 추가만, 수정 금지 |
| 반복 | R1·R2 **N=2**(운영 정책 N=2와 동형 — 결과가 곧 운영 예측) / R4 분산 측정 N=6 / R6 N=6~8 |

### 통제 조건

- 동일 셀카 파일·동일 상품컷·단일 pv(R2 중 동결)·단일 채점자(Dev A).
- 출력은 3:4 세로 목표. 프로바이더가 미지원하면 최근접 비율 사용하고 manifest에 기록.
- 프로바이더별 API 문법 차이는 어댑터가 흡수하되 **지시의 의미는 불변**. 이탈 시 manifest에 기록.

### 셀 정의

셀 = (셀카 × 상품). 식별자 `{model}__{selfie}__{sku}__{pv}__r{n}` (파일명 겸용). R2 본 매트릭스 = 2모델 × 9셀 × N2 = 36장 + 스카프 2모델 × 1셀 × N2 = 4장.

---

## 3. 단계별 계획 R0~R6

### 요약표

| 단계 | 목적 | 종료 기준(게이트) | 타임박스 | 장수 | 예상 비용 |
|---|---|---|---|---|---|
| R0 준비 | 키 2종·자산·툴 코어 | fake로 run→gallery→report 왕복 + 각 키 1콜 검증 | 4~5h | 0~2 | ~$0 |
| R1 스모크 | 프로바이더 생존성 | **G1** 진행 모델 확정 | 1~2h | 4 | <$1 |
| R2 본 베이크오프 | 승자·백업·Go/No-Go·스카프·§7 | **G2(최우선)** 산출물 1~3 확정 + 00 §7 기입 커밋 + B 통지 | 3~5h | ~40 | $2~5 |
| R3 프롬프트 심화 | pv2 확정 | 축별 결론 + pv2 동결 | 2~3h | 32~48 | $2~7 |
| R4 안정화 | 분산·엣지·실패 분류 | **G3** N·재시도·타임아웃 배분 동결 | 1.5~2h | ~24 | $1~4 |
| R5 실엔진 이관 | 승격·배포 | **G4** Railway ENGINE 교체 E2E + 롤백 검증 + B 통지 | 2~3h | ~4 | <$1 |
| R6 데모 자산 | p1~p3 납품 | 페르소나별 전항목 pass 1장 → public/demo 커밋 | 2~3h | 24~32 | $1~5 |

총 ~130~150장 / **$5~20 예상** (하드 가드 $50, §6). 3~4일 배치: **D1** = R0+R1(+툴 빌드 B1~B4) / **D2** = R2 → §7 기입·B 통지 → R3 착수 / **D3** = R3 마무리 + R4 + R5 / **D4** = R6 + 버퍼.

### R0 준비 — 체크리스트

- [ ] OpenAI·Google AI 키 결제 확인, 각 1콜 스모크, **이미지 편집 모델 ID 최신 확인 → `models.yaml` 기입** (모델명은 코드가 아니라 설정 문자열 — 06 §3)
- [ ] 상품컷: 베이크오프 3종 + **스카프(MEFGAMM12CO001) 공식몰 컷 직접 저장** (B 대기 금지 — 01 §2 관례), `prep_assets`로 정규화
- [ ] 셀카 3장 확보(동의 확인) → `assets/selfies/`, 레지스트리 기입
- [ ] 툴 B1~B3 완성, fake 프로바이더로 $0 E2E (06 §12)
- [ ] 루트 `.gitignore`에 `research/assets/`·`research/experiments/` 추가 (실인물·공식몰 이미지 미추적)

### R1 스모크 — 생존성 판정 (1셀카 × 1상품 × 2모델 × N2 = 4장)

입력 권장: 셀카 smart_daily × 상품 No.09(비세토스 — 가장 어려운 패턴 재현). 생존 기준: **정책 거부 없이 1장 이상 유의미한 합성 산출**. 생존 판정과 별개로 6가지 관찰을 기록한다:

1. **실인물 얼굴 편집에 대한 정책 거부 여부 (최우선)** — 프로바이더별로 상이하며, 여기서 후보가 통째로 탈락할 수 있다
2. 로고·상표 재현 거부 또는 임의 변형
3. 다중 이미지 입력 지원 방식(장수 제한·전달 순서)
4. 지원 출력 사이즈·종횡비(3:4 가능한가)
5. 가시 워터마크 유무
6. 대략적 지연

**G1 판정**: 2종 생존 → R2를 2모델로 / 1종 생존 → 단독 진행 + "백업 부재" 리스크 기록, 컨틴전시 후보 1종 병행 검토 / **0종 생존 → 컨틴전시 발동: fal.ai 키 발급 + FLUX Kontext 어댑터 추가(httpx REST, SDK 불요 — 06 §4) 후 R1 재실행**. 컨틴전시까지 전멸이면 §5의 전멸 시나리오.

### R2 본 베이크오프

1. `run configs/r2_bakeoff.yaml` (동시성으로 벽시계 30~60분) + `r2b_scarf.yaml`
2. 갤러리 채점 45~60분 (§4 프로토콜)
3. `report` → 순위 제안 확인 → **승자·백업·Go/No-Go·스카프 판정** (§5 규칙)
4. 부록 B 템플릿으로 **00-common §7.1/§7.2 기입 + 미결② 해소 + 본 문서 부록 A 기록 — 같은 커밋**, B에게 통지

### R3 프롬프트 심화 — 승자 모델만, 축별 독립 1:1 비교 (full factorial 금지)

| 우선 | 축 | 비교 |
|---|---|---|
| **0** | **⑥ cross·shoulder 스트랩 배치 지시** (2026-08-16 신설) | pv1 vs 스트랩 경로를 단계로 쪼갠 지시 |
| — | ① ref 이미지 장수·순서 | ~~1장 vs 2~3장~~ → **R2c로 선행 확정 (§A-3)** |
| ~~1~~ | ~~⑤ 품질 티어~~ → **폐기 (§A-13 ⑥)** | `gpt_high` 유일 성공이 **114.3s** — 지연 게이트에서 탈락, 라이브 불가. R6도 `gpt_base`로 간다 |
| 2 | ② 부정 지시 블록 | 유 vs 무 (로고 변형·신체 변형 금지문) |
| 3 | ④ style_hints | lighting만 vs 미사용 — city 무드는 v1 톤보정 정책상 최소 활용(밀라노=스트리트 등 v2 정의 주의) |
| 4 | ③ 프롬프트 언어 | pv1(영어 템플릿+한국어 DB 원문 임베드) vs 전량 영어 번역 |

축당 대표 셀 2~3개 × N2. 산출: **pv2 동결** + 축별 결론 한 줄(부록 A).

> **★ R3 종료 (2026-08-18) — 아래 축 표는 역사적 기록이다.** 축⑥: `pv1c` 기각(§A-10) → `pv1d`
> **미판정-기각**(표본 결손) → **정지 규칙 발동**. cross에 대한 프롬프트 축은 공식 종결이고,
> **산출물 ④는 pv2 = pv1(변경 0)로 동결**됐다 (§A-13 ⑤⑦). 축⑤는 폐기(§A-13 ⑥), 축②③④는
> 미착수 상태로 종료한다. 아래 서술은 당시의 판단 근거를 보존하기 위해 그대로 둔다.
> **애초에 0순위였던 이유 (§A-9 ①)**: gpt 40장 기준 cross 전항목 pass **5/18(28%)**이고,
> 실패가 **정면·직립·팔 내림 자세에 몰려 있다**(s_casual 1/8 · s_smart_lowres 0/6 vs
> s_formal 2/2 · s_smart 2/2). 두 벤더가 **같은 방식으로** 실패한다 —
> **팔과 몸통 사이 틈으로 보여야 할 스트랩 구간을 그리지 못한다.**
> 따라서 축⑥의 과녁은 "스트랩을 그려라"가 아니라 **"팔–몸통 틈으로 스트랩을 통과시켜라"**다.
> 게다가 **간판 페르소나 p1의 확정 SKU가 그 cross 백(No.09)**이다 — 이 축이 R6 자산 확보 비용을
> 직접 결정한다. 축① 대비 우선순위가 아니라, **다른 모든 축보다 먼저**다.
>
> 다만 낙관은 금물이다: pv1의 cross 지시문은 (A)연속성 (B)팔 뒤로 통과 (C)어깨→반대쪽 힙을
> **이미 전부 명시하고 있고 셋 다 지켜지지 않았다.** 즉 "지시가 없어서"가 아니라 "지시가
> 먹히지 않아서"이므로, **문구를 더 쓰는 것으로 풀린다는 보장이 없다.** 축⑥이 실패하면
> 남는 카드는 (i) 정면 셀카에서 cross를 피하는 §7.2 조건 추가 (ii) R6 best-of-N 물량
> (iii) p1 SKU 교체를 B와 협의 — 순서대로 검토한다.

### R4 안정화

- **분산**: 최고 셀·경계 셀 2개를 N6 반복 → 전항목 pass율 p 추정 → **라이브 N=2 성공률 = 1-(1-p)²** 산출 (이 숫자가 데모 리스크의 실측치)
- ~~**back 위치 엣지 심화**: 정면 셀카에서 스트랩 표현 관찰 — 실패 경향이면 §7.2에 "백팩은 반측면 이상 권장" 반영~~ → **과녁이 틀렸다 (§A-9 ②).** back 실패의 원인은 사용자 사진(§7.2)이 아니라 **상품 레퍼런스(§7.1)**였다: 정면 팩샷에 스트랩이 안 보여 모델이 색을 지어낸다. ref 1장 1/6 → ref 2장 4/4. R4에서는 **cross 자세 엣지**로 대체한다
- **실패 유형 분류**: refused / fake-product / distortion / position-ignored / timeout / network → 유형별 엔진 대응 확정: network·timeout → 재시도 1회 / refused → 프롬프트 순화 변형 1회 / 품질 실패 → **N=2 병렬 first-ok** (v1에 자동 품질 판정이 없으므로 "N=2 생성 후 선택"(01 §4)의 현실화. VLM judge 선별은 P1)
- **타임아웃 배분 동결**: 다운로드 5s + 생성 40s + 업로드 5s + 여유 5s = 55s (`engines/base.py`의 `default_timeout_seconds=55.0`과 정합)

### R5 실엔진 이관 — 06 §11 절차

복사-이식 → `_BUILDERS` lazy 등록 → requirements 최소 추가 → Railway env(`ENGINE={winner}` + 승자 키 + `SUPABASE_*`) → curl 스위트 + 실생성 E2E 1건 → **롤백 리허설(`ENGINE=mock` 원복만으로 복구되는지)**. 덤: `services/gen/static/samples/`를 베이크오프 합격작으로 교체(mock 폴백 화면도 리얼해짐).

### R6 데모 자산 — 러너 재사용

`r6_personas.yaml`(모델=승자, 셀=p1~p3 × 확정 SKU, N6~8, pv2) → 갤러리에서 **6항목 전부 pass 1장씩 선별** → `prep_assets`로 3:4 정리 → `apps/web/public/demo/{p1..p3}/photo.jpg·result.jpg·meta.json`(태그 3종·SKU·추천 이유 1줄) 커밋. p4~p6은 미결③(도시 데이터) 도착 시 config 추가만으로 재실행.

---

## 4. 평가 프로토콜

체크리스트 6항목 = 01 §2 원문에 안정 ID 부여. **항목별 fail 기준** — 하나라도 해당하면 fail:

| ID | 항목 | fail 기준 |
|---|---|---|
| c1 | 패턴·로고·하드웨어 톤 일치 | 비세토스 반복 단위 왜곡 / 로고 플레이트 문자 뭉개짐 / 골드·실버 뒤바뀜 / 컬러(꼬냑 등) 이탈 |
| c2 | 크기·착용 비율 타당 | DB 사이즈 대비 명백한 과대·과소 (미디엄 크로스바디가 토트 크기 등) |
| c3 | 지정 wear_position 합성 | 지정 위치가 아닌 곳에 합성 / back인데 스트랩 표현조차 없음 / **(개정 2026-08-16)** cross·shoulder인데 ⓐ 어깨~가방 사이 스트랩 구간이 누락됐거나 ⓑ 스트랩이 팔·몸통을 관통하거나 ⓒ 좌우가 지시와 반대다 |
| c4 | 인물 보존 | 얼굴 왜곡·동일성 훼손 / 손가락 왜곡 / 의상 임의 변형 |
| c5 | 조명·그림자 융합 + 원본 배경 유지 | 배경 변형 / 그림자 없음·방향 불일치로 제품이 "붙인 티" |
| c6 | 화보 톤 | 광고 화보로 못 쓸 어색함 (주관 — 보수 판정) |

> **c3 개정 이력 (2026-08-16, 사유 = 부록 A-7 ②·⑤)**: 원래 기준은 `back`의 스트랩 누락만 적고 있어
> **cross·shoulder의 스트랩 결함을 판정할 규칙이 없었다.** 그 결과 채점자가 결함을 note로
> 적어 놓고도 c3에 값을 넣지 못한 이미지가 **10장** 나왔고(전부 cross·shoulder, back은 0건),
> `report`가 그 10장을 통째로 집계에서 제외했다. ⓐⓑⓒ는 전부 pv1이 명시적으로 금지한 것이므로
> 새 요구가 아니라 **원래 의도의 명문화**다.
> ⚠ **이 개정은 R2와의 비교 가능성을 끊는다** — R2는 구두 일괄 판정이었고 개정 전 기준을 썼다.
> R2의 c3 통과율과 이후 단계의 c3 통과율을 나란히 놓지 말 것.

**채점 규칙**: 이미지(rep) 단위 채점 / 단일 채점자(Dev A — 일관성) / **애매하면 fail** / note 필드에 실패 사유 한 줄 의무(→ §7 스펙·R4 실패 분류의 원천 데이터). **셀 통과 = N개 중 ≥1장이 6항목 전부 pass** (best-of-N — 운영 N-best와 동형). 도구(report)는 순위를 "제안"만 하고, 확정은 A가 갤러리 재확인 후. 승자 확정 전 B의 30분 스팟체크 권장(결정권은 A).

---

## 5. 승자 결정 규칙 · Go/No-Go · 스카프

### 부적격 게이트 (순위 무관 승자 불가)

- 거부+오류율 > 20% (R2 20회 중 4회 초과)
- 단건 생성 p95 > 40s (55s 예산 − I/O ~10s − 여유)
- 제거 불가한 가시 워터마크

### 타이브레이커 사슬 (주 지표 동점 시 순서대로)

**주 지표 = 셀 통과 수(9셀 중)** → ① c1+c2 통과 수(제품 정합성) → ② c4(인물 보존) → ③ c3+c5(위치+융합) → ④ c6(화보 톤) → ⑤ p95 낮은 쪽 → ⑥ 단가 낮은 쪽 → ⑦ 운영성 판단 한 줄 기록. (01 §0 품질 우선순위와 1:1 — 자의성 제거, 결정 재현 가능.)

**백업 모델** = 순위 2위. 2모델 구도에서는 자연히 타 벤더가 되어 단일 장애점을 회피한다(발표 당일 한 벤더 장애 대비).

### Go / No-Go (기본값 — 판정 시 조정 가능하나 조정 사유를 부록 A에 기록)

| 판정 | 조건 | 행동 |
|---|---|---|
| **GO** | 승자 셀 통과 ≥ 6/9 AND c1 통과율 ≥ 70% AND 부적격 게이트 통과 | R3~R6 전체 진행 |
| **CONDITIONAL GO** | 셀 통과 4~5/9 | 제한 GO — 약한 wear_position 제외, §7.2 사진 조건 강화, 제한 내용을 데모 시나리오에 반영 |
| **NO-GO** | 셀 통과 < 4/9 또는 "그럴듯한 가짜 제품"이 구조적 | 라이브 생성 격하(`ENGINE=mock` 유지), 사전 생성 중심. **R6는 최선 모델로 계속**(대량 뽑기+선별은 여전히 성립). B의 계약·플로우 불변 — B를 끌어들이지 않는다 |
| (전멸) | 컨틴전시 포함 전 모델 정책 거부 | 데모 자산 자체가 위험 → 스코프 축소 피벗(A 주도) |

### 스카프 판정 (미결② 해소)

승자 모델 출력 중 ≥1장이 **c1·c3(자연스러운 목 배치)·c4·c5 동시 pass**면 포함. **애매하면 제외 — 디폴트 편향 = 제외.** '제외'도 미결②의 유효한 해소이며, 00-common §8 neck 주석과 B의 matchable 플래그에 반영을 통지한다.

---

## 6. 비용 · 볼륨 계획

- 단가 가정(전부 `models.yaml`의 `cost_per_image_usd`로 조정 가능한 추정치): Gemini 이미지 편집 ~$0.04 / gpt-image mid ~$0.07, high ~$0.19 / (컨틴전시) FLUX Kontext ~$0.04~0.08
- 총 ~130~150장, **$5~20 예상. 하드 가드 $50** — 초과 시 즉시 중단하고 계획 재검토
- 절차적 안전장치: 모든 실행 전 `--dry-run` 견적 확인 의무 + config별 `budget_cap_usd`(초과 시 러너가 실행 거부)

---

## 7. 리스크 레지스터

| 리스크 | 징후 | 완화 | 트리거 시 행동 |
|---|---|---|---|
| 실인물 얼굴 편집 정책 거부 | R1 refused | R1 조기검증 / "편집" 프레이밍 프롬프트 | G1 컨틴전시 → 그래도 전멸이면 §5 전멸 시나리오 |
| 로고·상표 재현 거부/변형 | c1 연쇄 fail | "레퍼런스의 제품을 그대로"(생성 아닌 편집) 프레이밍 | 백업 모델 전환 |
| 레이트리밋·신규 쿼터 | 429 | R0 티어 확인 / 동시성 per-model 2 / 백오프 / resume | 동시성 하향 재실행 |
| 비용 폭주 | dry-run 견적 초과 | budget_cap + $50 가드 + 단가 레지스트리 | 실행 중단·볼륨 축소 |
| 프로바이더 장애·지연 스파이크 | timeout 다발 | resume / 기록 후 스킵 | 백업 모델로 진행 |
| 출력 종횡비 제약 | 3:4 불가 | R1 기록 | 크롭 후처리 + §7 스펙 반영 |
| 가시 워터마크 | R1 확인 | — | 부적격 게이트 |
| p95 > 40s | R2 지연 통계 | N=2 병렬 | 부적격 게이트 |
| **자기 회선 열화를 프로바이더 지연으로 오인** (2026-08-17 실측) | 204 응답에 2.5~12.7s. 그 상태의 측정치로 "프로바이더 +23% 드리프트" 결론을 썼다가 철회 | 러너에 네트워크 사전점검 추가 — 생성 전에 무과금 경로 RTT를 잰다 | 회선 이상 시 **실행 중단**. 그 세션의 지연 데이터는 폐기하고 품질 데이터만 채택 (§A-11) |
| **60초 계약이 네트워크 상태에 종속** (2026-08-18 실측) | 회선 열화 세션에서 gpt mid **23시도 중 60초 내 성공 0**, 성공 14건 전원 61.9~97.9s. **최속이 61.9s라 N 병렬로도 구제 불가** (§A-13 ②③) | 정상 회선 여유는 4.1s뿐(§A-6 ④) — 여유가 아니라 **폴백이 방어선**이다. 실행 전 `probe_network`(§A-11) | **B의 데모 모드 폴백을 예외가 아니라 상시 경로로 취급**. 발표장 네트워크 사전 실측 + `?demo=1` 토글 준비 확인 (B 통지 (a)(b)) |
| **cross 스트랩 — 특정 자세에서 붕괴** (2026-08-17 실측) | cross 전항목 pass **5/18=28%**, 라이브 N=2 **48%**. **정면·직립·팔 내림 자세에서만** (s_casual 1/8 · s_smart_lowres 0/6 vs s_formal 2/2 · s_smart 2/2). 결함 = 팔–몸통 틈으로 보여야 할 스트랩 구간 누락 (§A-9 ①) | ~~R3 축⑥~~ **실패(§A-10)**. 프롬프트 시도는 `pv1d` 한 번 남았고 정지 규칙 확정 | **① §7.2 자세 조건 = 1순위 확정(§A-10 ⑤)** ② R6 best-of-N 물량 ③ p1 SKU 교체를 B와 협의 |
| **c3 판정 불능** (2026-08-16 해소) | c3 공란 10건이 전부 cross·shoulder에 집중, back은 0건 | §4 c3 기준 개정 + 갤러리 툴팁 동기화 | **R3 착수 전 공란 10장 재채점 (선행 조건)** |
| 타임박스 슬립 | D2 내 G2 미달 | 게이트별 디폴트 액션 | 현재까지 최고 모델로 Go, R3 축소 — §7 기입은 불가침 |
| 셀카 개인정보 | — | 동의된 사진만 / gitignore / 전송 전 리사이즈 | — |
| 미결③ 미도착 (도쿄·파리·뮌헨) | — | A 리스크 아님(의존성) — p4~p6만 차단 | 도착 시 R6 config 추가 |

---

## 8. 문서 갱신 의무 ("같은 커밋" 원칙)

| 트리거 | 대상 | 내용 |
|---|---|---|
| **R2 종료 즉시** | 00-common §7.1/§7.2 | 입력 스펙 기입 (부록 B 템플릿 → 확정값) |
| 〃 | 00-common 미결 목록·§8 | 미결② 해소, neck 주석 갱신 |
| 〃 | 본 문서 부록 A | 승자·백업·Go/No-Go·스카프 + report 요약표 붙여넣기 |
| 〃 | Dev B | 블로킹 해제 통지 (대량 이미지 수집 시작 신호) |
| **R3 종료 (2026-08-18)** | 00-common §7.1 | 스트랩 카테고리 일반화 개정 — `cross`·`shoulder`·`back`은 **스트랩 선명 컷 1장 필수** (§A-13 ⑧) |
| 〃 | 본 문서 부록 A-13 + 결정 기록 표 | 산출물 ④ 동결(pv2=pv1) · 축⑥ 종결 · 축⑤ 폐기 · 60초 계약 조건부 명기 |
| 〃 | Dev B | **폴백 상시화 통지**(60초 계약이 네트워크 상태에 종속) + §7.1 개정 + `?demo=1` 토글 구현 여부 확인 |
| R5 종료 | 00-common §10 env 표 | "이미지 생성 API 키" 변수명 구체화 |
| 〃 | services/gen/README | 현재 엔진 표기 갱신 |

03-mock-service-spec.md(계약 문서)는 불변. 미결①(피칭 문구)은 A 소관 아님.

---

## 9. 안티스코프 (이 연구에서 하지 않는 것)

파인튜닝·LoRA·셀프호스팅(원천 금지) / 배경 교체 실험(P2 스트레치) / 의류(garment) / 멀티 앵글 / 속도 최적화 작업(로딩 연출이 커버) / 통계적 유의성 추구(N=2는 방향타 — 결정은 데이터+판단) / **자동 품질 지표(CLIP·SSIM 등) 일체** — 1순위 기준 "그 로고가 정확히 그 로고인가"는 자동 지표가 가장 못 하는 판별이고, ~150장 × 6항목은 갤러리로 1~2시간 인간 채점이면 끝난다 / 연구 단계 VLM judge(승자 확정 후 엔진 P1 기능으로만) / 계약(00 §5) 변경 일체.

---

## 부록 A. 결정 기록 (실행하며 채움 — 이 표가 유일한 영구 기록)

| 항목 | 결정 | 근거 | 일시 |
|---|---|---|---|
| R1 생존 모델 | **2종 생존** (gemini_nb · gpt_image) | 스모크 4/4 ok, refused 0. 정책 거부 미관측 | 2026-08-15 |
| R2 승자 / 백업 | **승자 gpt_image** (gpt-image-2, quality=mid, 1152×1536) / **백업 gemini_nb** | 아래 §A-1 표 + §A-2 게이트 조정 | 2026-08-16 |
| Go / No-Go | **GO (문턱에 걸침)** | R2 36장 전면 재채점 확정: gpt **6/9** · c1 83%(≥70%) → GO 유지. ~~9/9~~는 구두 일괄 판정의 산물. 실패 3셀 중 2셀이 `back`이고 **레퍼런스 부족이 원인**이라 ref 2장 배포 설정에서 **8/9 전망**. ⚠ c1은 문턱에서 6%p 안쪽 (§A-1 개정 · §A-9) | 2026-08-17 |
| 스카프 포함 여부 | **포함** | 승자 출력이 c1·c3·c4·c5 동시 pass. 미결② 해소 | 2026-08-16 |
| §7.1 / §7.2 기입값 | 00-common §7.1/§7.2 기입 완료. **§7.1은 이후 2회 개정** — ① `back` 보조컷 사실상 필수(§A-9 ②) → ② **스트랩 카테고리 전반(`cross`·`shoulder`·`back`)에 "스트랩 선명 컷 1장 필수"로 일반화**(§A-13 ⑧) | §A-3 · §A-9 ② · §A-13 ⑧ | 2026-08-18 |
| 실엔진 설정 (R5 이관 대상) | **확정: `gpt_base` = quality=mid · size=1152×1536 · ref 2장 · 병렬 N호출** (=현행 유지). `input_fidelity` 실행 불가 / `n=2` 단일호출 기각 / **`912×1216` 미채택(휴면)** / **quality=high 폐기**(§A-13 ⑥) | §A-6 + §A-7 ① + §A-13 ⑥ | 2026-08-18 |
| pv2 diff 요약 (축별 결론) | **확정: pv2 = pv1 — diff 없음.** 축① 확정(§A-3) / 축⑥ cross: `pv1c` 기각(§A-10) → `pv1d` **미판정-기각**, 정지 규칙 발동으로 **프롬프트 축 종결**(§A-13 ⑤) / 축⑤ 품질 티어 **폐기**(§A-13 ⑥) / 축②③④ **미착수 상태로 종료** | §A-13 ⑤⑥⑦ | 2026-08-18 |
| pv2 확정 규칙 (누적) | cross 지시는 `one shoulder → opposite hip` **상대 지시 형태를 보존**한다. 절대 좌우·기준점 없는 상대어 금지. *(pv2=pv1으로 동결됐으므로 이 규칙은 향후 누가 프롬프트를 건드릴 때의 제약으로만 산다)* | §A-10 ② | 2026-08-17 |
| N / 재시도 / 타임아웃 정책 | **동결: N=2 병렬 first-ok · 재시도 1회(network·timeout) · 엔진 타임아웃 55s.** N=3은 카드로 보류(§A-6 ④). ⚠ **60초 준수는 회선·프로바이더 상태에 의존한다** — 열화 세션 실측 **23시도 중 60초 내 성공 0**(§A-13 ②) | §A-6 ② · §A-13 ⑦ | 2026-08-18 |

### A-1. R2 실행·채점 요약 (report 발췌)

R2 = r2_bakeoff(2모델 × 셀카3 × 상품3 × N2 = 36장) + r2b_scarf(2모델 × 1셀 × N2 = 4장).
실지출 $3.40. **40장 전량 ok — refused 0 · error 0 · timeout 0.**

| 모델 | 시도 | ok | refused | error | timeout | 거부+오류율 | p50 | p95 | Σ비용(추정) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| gpt_image | 18 | 18 | 0 | 0 | 0 | 0.0% | 41.1s | 55.5s | $1.44 |
| gemini_nb | 18 | 18 | 0 | 0 | 0 | 0.0% | 12.4s | 18.4s | $1.62 |

| 모델 | 셀 통과 | 채점 이미지 | c1 | c2 | c3 | c4 | c5 | c6 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| gpt_image | **9/9** | 18 | 100% | 100% | 100% | 100% | 100% | 100% |
| gemini_nb | 0/9 | 0 | — | — | — | — | — | — |

> **⚠ gemini_nb의 0/9는 품질 실패가 아니라 미채점이다.** 스폰서 제약으로 승자가 gpt_image로
> 먼저 확정되어, 백업 모델은 채점 대상에서 제외했다. 실행 통계(거부·오류·지연)는 전량 유효하다.
>
> **⚠ 채점 방식**: 갤러리 육안 확인 후 **구두 일괄 판정**(전 항목 pass)이며, 이미지별 개별
> 체크가 아니다. scores.json은 그 판정을 전사한 것으로 `scorer` 필드에 명시돼 있다.
> fail 0건이므로 §4의 "fail마다 note 한 줄" 의무는 발동하지 않았고, 결과적으로
> **§7 스펙을 *강화*할 관측 근거는 이번 실행에서 나오지 않았다**(§A-3 참조).

> ## ★ 2026-08-17 전면 재채점 — **위 두 표는 폐기됐다**
>
> 36장 전량을 개정 c3 기준으로 **이미지별 개별 채점**했다(gemini 18장 포함 — 위 "미채점"
> 상태 해소). 위 채점표는 역사적 기록으로만 남기고, **아래가 R2의 확정값**이다.
>
> | 모델 | 셀 통과 | 전항목 pass | c1 | c2 | c3 | c4 | c5 | c6 |
> |---|---:|---:|---:|---:|---:|---:|---:|---:|
> | gpt_image | **6/9** | 10/18 | 83% | 83% | 89% | 100% | 100% | 100% |
> | gemini_nb | **8/9** | 12/18 | 100% | 83% | 89% | 94% | 100% | 100% |
>
> **① 구두 일괄 판정은 9/9를, 개별 채점은 6/9를 냈다.** A-1이 경고했던 방법론 한계가
> 실제로 **3셀만큼** 결론을 부풀리고 있었다. 실행 통계(지연·거부·오류)는 영향 없다.
>
> **② 백업 모델이 주 지표에서 승자보다 높다 (8/9 vs 6/9, c1 100% vs 83%).**
> **승자는 바뀌지 않는다** — 스폰서 제약(OpenAI)으로 확정된 사안이며 품질 비교의 대상이 아니다.
> 다만 백업이 실제로 강하다는 사실은 **장애 시 폴백의 가치가 기록보다 크다**는 뜻이고,
> §5의 "2모델 구도로 단일 장애점 회피" 판단을 사후적으로 정당화한다.
>
> **③ gpt의 실패 3셀 중 2셀이 `back`이고, 그 원인은 모델이 아니라 레퍼런스다** — §A-8 참조.
> R2는 `max_ref_images: 1`(정면 팩샷만)로 돌았고, back 실패 5건의 note가 전부
> "스트랩 색이 다름"이다. **정면 팩샷에는 어깨 스트랩이 안 보이므로 모델이 색을 지어냈다.**
> 보조컷을 준 S4a에서는 back이 4/4 전항목 pass다. → **R5 배포 설정(ref 2장)에서는 8/9로 회복될 전망.**

### A-2. 부적격 게이트 ② 조정 (§5 기본값 조정 — 사유 기록 의무 이행)

기본값 "단건 생성 p95 > 40s"를 **"N=2 병렬 first-ok 기준 총 소요 > 60s"**로 재유도한다.

- **근거**: 40s는 55s 내부 배분에서 파생된 값이고, 계약상 실제 상한은 00-common §5 폴링
  규약의 **60초**다(B 최대 대기 60s / A 60초 내 done·failed 종결 의무).
- **실측**: gpt_image 단건 p95 55.5s(n=18) → N=2 병렬 first-ok p95 **46.2s**(경험분포).
  I/O 10s 가정 시 총 56.2s로 60초 계약 충족(여유 3.8s). R2c의 같은-조건 재현에서는
  단건 p95 **43.1s**로, 55.5s가 이상치였음이 확인됐다.
- **미검증 잔여**: 다운로드·업로드 실측치가 없다. I/O가 15s를 넘으면 계약 초과율이 11%로
  오른다 → **R5 배포 E2E에서 I/O를 실측해 이 판정을 재확인한다.** ~~여유가 부족하면
  출력 크기 축소(912×1216, 정확히 3:4·16배수, gemini와 동일 화소)를 카드로 남겨 둔다.~~
  → **이 카드는 §A-6 ①에서 폐기됐다** (실측: 화소 −37%인데 지연 −0.8s. 지연이 화소에
  묶여 있지 않다). 대체 카드는 **N 상향**이다 — §A-6 ④. 아울러 first-ok p95는 표본을
  40건으로 늘려 46.2s → **45.9s**로 갱신됐다(여유 3.8s → 4.1s).
- **부수**: gemini_nb를 백업으로 유지해 벤더 단일 장애점을 회피(§5).
- 게이트 ①(거부+오류율) 0.0%, 게이트 ③(가시 워터마크) 미관측 — 조정 없이 통과.

> **도구 주의**: `report.py`는 §5 **기본값** 40s를 하드코딩하고 있어, 자동 출력은
> gpt_image를 부적격 처리하고 "승자 gemini_nb / NO-GO"라는 잘못된 제안을 낸다.
> `MAX_P95_MS`를 고치지 않은 것은 의도적이다 — 코드를 바꾸면 R3·R4에서 조정된 규칙이
> 조용히 적용되어 §5 원문과 코드가 어긋난 이유를 추적할 수 없게 된다. 조정은 이 기록으로만 산다.

### A-3. R2c — 레퍼런스 장수 축 (R3 축① 선행 확정)

§7.1의 "3/4 각도 1장 권장"을 그대로 확정해도 되는지 검증. 이 문구가 **B의 전 카탈로그
수집 분량**을 정하므로 틀렸을 때의 재작업 비용이 가장 크다.
config `r2c_refcount.yaml` — gpt 단독, `max_ref_images`만 다른 1:1(gpt_ref1/gpt_ref2),
3 SKU × s_casual × N2 = 12장, $0.96, 12/12 ok.

| | ref_count | p50 | p95 | 입력 토큰 |
|---|---:|---:|---:|---:|
| gpt_ref1 (정면만) | 1 | 40.2s | 43.1s | 2683~2852 |
| gpt_ref2 (정면+보조컷) | 2 | 39.1s | 42.8s | 3554~4123 |

**결론 (육안 1:1 대조 — 체크리스트가 아닌 R3 축별 결론 형식):**

1. **제품 동일성이 확실히 개선됐다.** 단 R2가 이미 9/9 전항목 pass라 c1이 천장에 붙어 있어,
   **체크리스트로는 잡히지 않는 합격선 위쪽의 개선**이다. → §7.1을 "필수"가 아니라 **"권장"**으로 확정.
2. **패턴 회귀 없음.** 크로스 보조컷이 578×842로 1024 하한 미달인데 비세토스 패턴이 유지됐다.
   표본이 1 SKU × 2장이라 **하한을 낮추는 근거로는 약하다** — 초안값 1024를 유지하고 관측만 기록.
3. **[음성 결과] 백팩 스트랩 표현은 개선되지 않았다.** 정면 팩샷에 어깨 스트랩이 없으므로
   후면 등판컷을 주면 c3의 back 표현이 좋아지리라 가정했으나 **반증됐다.**
   → **back 스트랩은 레퍼런스 이미지로 해결되지 않는다. 프롬프트 측 문제로 확정하고 R3(배치 지시)·
   R4(back 엣지)로 이관한다.** "레퍼런스를 더 주면 되지 않을까"를 재시도하지 말 것.
4. 각도 종류(사선 3/4·탑다운·후면)별 우위가 관측되지 않아 **§7.1에서 각도를 지정하지 않는다.**
5. 지연 페널티 없음, 비용 +$0.01/장 수준 — 보조컷 추가의 운영 비용은 무시할 만하다.

### A-4. 자산 처리 기록

- `s_smart`를 R2 착수 직전 교체(구 380×1244 → 1440×1800). 교체분이 **확장자만 `.jpg`인 WebP**
  였고 두 어댑터 모두 입력 MIME을 `image/jpeg`로 하드코딩하므로, 그대로 실행했다면 s_smart 행
  16셀이 400 위험에 노출됐다(= 33% error → 게이트 ①을 우리 자산으로 자초). `prep_assets`로
  1229×1536 진짜 JPEG로 정규화 후 실행. WebP 원본은 `assets/_originals/`에 보관.
  `prep_assets --verify`는 확장자만 보므로 이 사고를 잡지 못한다 — 알려진 구멍.
- s_smart는 룩북성 촬영물이다. 실사용자 셀카는 s_casual·s_formal 2장(40장 중 24장 생성분)이며,
  **§7.2가 실사용자 조건 대비 낙관 편향일 수 있다.** R4/R6 착수 전 실사용자 셀카 추가 확보 권장.

### A-5. S4a/S4b 설정 최적화 — **판정 프로토콜 (실행 전 사전 확정)**

> 이 절은 **이미지가 한 장도 생성되기 전에** 쓰였다. 사후에 규칙을 고르면 어떤 결과든
> 원하는 결론으로 읽히기 때문이다. R2c는 체크리스트가 안 먹히자 *사후에* 육안 대조로
> 답을 구했고 — 그래서 실험이 살긴 했지만 — 그 판정은 재현 규칙이 없다. 이번엔 먼저 못 박는다.

**문제**: R2가 9/9 전항목 pass다. c1~c6 이진 체크리스트는 **천장에 붙어 있어 변형 간 구별을
하지 못한다**(R2c에서 이미 겪음). 그런데 이번 산출물은 "합격/불합격"이 아니라 **"넷 중 어느
설정이 더 나은가"**라는 순위 질문이다. 도구를 바꿔야 한다.

**해법 — 2층 판정.** 절대 판정과 상대 판정을 분리하고, 서로 다른 것을 재게 한다.

| 층 | 도구 | 재는 것 | 판정 단위 |
|---|---|---|---|
| **1 — 바닥(회귀)** | 기존 갤러리 c1~c6 | 변형이 **합격선 아래로 떨어지는가** | 이미지별 개별 체크 |
| **2 — 순위(우열)** | 강제 선택 쌍대 비교 | 합격선 **위쪽에서의 우열** | 쌍(pair)별 강제 선택 |

**층 1 규칙** — 05 §4 그대로(애매하면 fail, fail이면 note 한 줄 의무). 단 **R2의 구두 일괄
판정을 반복하지 않는다**(A-1의 방법론 결함). 이미지별로 개별 체크한다. 난도 셀(s4b)에서
나오는 fail의 note가 **§7.2를 강화할 유일한 관측 근거**다 — A-1이 "이번 실행에서 나오지
않았다"고 적은 그 데이터를 여기서 만든다.

**층 2 규칙** — `genlab/compare.py`가 생성하는 강제 선택 페이지.

- 각 변형 팔을 **baseline(gpt_base)과만** 짝짓는다. 4팔 전체 곱집합(6쌍)이 아니라 3쌍인
  이유: 우리가 답할 질문이 "baseline을 갈아탈 이유가 있는가"이지 팔들의 전순위가 아니다.
- **블라인드**: 좌/우 배치를 무작위화하고 팔 이름을 숨긴다. 판정이 끝난 뒤에만 정답이 열린다.
  (seed를 페이지에 기록해 사후 복원 가능.)
- **동점 금지**. 반드시 좌 또는 우. 이것이 천장을 뚫는 장치다 — "둘 다 pass"를 못 쓰게 만든다.
- **사유 태그 1개 필수**: 제품동일성 / 인물보존 / 융합(조명·그림자) / 화보톤 / 갈랐다고 보기 어려움.
  마지막 태그가 "사실상 무차별"의 정직한 출구이며, 강제 선택은 유지하되 그 표는 무차별로 집계된다.
  사유 태그가 있어야 이긴 이유가 **R3 프롬프트 축으로 이어진다**.

**채택 규칙 (사전 확정 — 이 숫자를 나중에 고치지 않는다)**: 팔당 8쌍(s4a 6 + s4b 2).

| 결과 | 판정 |
|---|---|
| 변형 승 ≥ 6/8 **AND** 층 1 회귀 없음 | 변형 채택 |
| 5/8 이하 (또는 "무차별" 태그가 과반) | **차이 없음으로 판정** → 아래 타이브레이커로 결정 |
| 층 1에서 회귀(합격선 이탈) 발생 | 승률과 무관하게 **변형 기각** |

**타이브레이커 (품질 무차별일 때, 순서 고정)**: ① N=2 first-ok p95가 낮은 쪽 → ② 단가가
낮은 쪽 → ③ baseline 유지(변경 없음이 기본값 — 근거 없는 변경은 R5 이관 리스크만 늘린다).

> **왜 승률에 유의성 검정을 붙이지 않는가**: n=8은 부호검정으로 유의성이 나오는 표본이 아니고,
> 05 §9가 **통계적 유의성 추구를 안티스코프로 명시**했다. 승률은 방향타이고 결정은 데이터+판단이다.
> 6/8이라는 문턱은 유의성이 아니라 "한쪽으로 확실히 기운 것처럼 보이는가"의 사전 합의값이다.

**gpt_n2 팔의 판정은 층 2를 쓰지 않는다.** 어댑터가 `data[0]`만 읽어 2번째 장을 버리므로
품질 팔이 아니라 **지연 팔**이다. 판정 기준은 오직 하나: `t_n2`(단일 호출)의 p95가
`min(t1,t2)`(2병렬 호출)의 p95보다 낮은가. 층 2 페이지에서는 제외한다.

**좌우 배치 seed = `s4-2`** (기본 `s4`에서 변경). 사유: `s4`는 baseline이 좌 2 / 우 6으로
쏠렸다. 8쌍에서 6-2 쏠림은 판정자의 좌우 편향과 팔이 교란된다. **판정이 한 건도 이뤄지기
전에**, 결과를 보지 않고, 층별로 균형(s4a 3/6 · s4b 1/2)이 되는 seed를 골랐다 —
결과가 아니라 **설계**를 고른 것이라 사후 선택이 아니다. seed는 페이지에 기록돼 배치를
정확히 복원할 수 있다.

**난도 축의 한계 (사전 명시)**: 이번에 올린 난도는 **입력 해상도 한 축**뿐이다.
§7.2 금기 3종 중 **역광·극단 각도·얼굴 가림은 자산이 없어 여전히 미검증**이다.
합성으로 만든 역광은 진짜 역광이 아니므로 §7.2의 근거로 쓸 수 없다 —
A-4의 "실사용자 셀카 추가 확보 권장"과 같은 항목으로 남긴다.

### A-6. S4a/S4b 실행 결과 — **지연·비용 축은 확정, 품질 축은 판정 대기**

이번 세션 지출 **$2.56**(레지스트리 단가 기준 견적 — 프로브 $0.24 포함, `gpt_fid`의 400 2건은
미과금이므로 제외) → **누적 ~$6.9 / 하드 가드 $50.** 실제 청구는 이보다 조금 낮다:
`gpt_s912`의 출력 토큰이 1344로 기준(1629)보다 적은데 단가는 과대 추정(0.08)을 유지했기 때문이다.
(러너의 `spent_usd`는 실패 시도에도 단가를 더한다 — 보수적인 방향이라 가드로서는 옳다.)
s4a 18/18 ok · s4b 6/6 ok · refused 0 · timeout 0.

| 팔 | 차이 | n | p50 | p95 | 출력 토큰 | 판정 |
|---|---|---:|---:|---:|---:|---|
| `gpt_base` | (대조군) 1152×1536 mid ref2 | 6 | 42.8s | 45.9s | 1629 | 기준 |
| `gpt_s912` | size 912×1216 | 6 | 40.6s | 48.4s | 1344 | **지연 이득 없음** — 아래 ① |
| `gpt_n2` | n=2 (단일 호출 2장) | 6 | 44.6s | 52.3s | 3257 | **기각** — 아래 ② |
| `gpt_fid` | input_fidelity=high | 0 | — | — | — | **실행 불가** — 아래 ③ |

> ⚠ 팔당 n=6이라 nearest-rank p95는 **사실상 6장 중 최댓값**이다. 상한 추정으로만 읽어라.
> 계약 판정은 아래 ④의 **n=40 통합 풀**을 쓴다.

**① 912×1216은 60초 계약의 지렛대가 아니다 — A-2가 남겨 둔 카드가 무효화됐다.**
화소는 −37%인데 **출력 토큰은 −17.5%**(1629→1344)뿐이고, 단건 p50은 통합 풀 41.4s 대비
40.6s로 **−0.8s**, p95는 오히려 높다(48.4s). 즉 이 모델의 지연은 **화소에 묶여 있지 않다.**
→ A-2의 "여유가 부족하면 출력 크기 축소(912×1216) 카드가 남아 있다"는 **폐기한다.**
여유가 부족할 때 남는 실질 수단은 **N을 올리는 것**뿐이다(④).
단가는 ~13% 싸므로, 품질이 무차별로 판정되면 비용 사유로 채택할 여지는 남는다(A-5 타이브레이커 ②).

**② 단일 `n=2` 호출은 2병렬 호출에 완패 — R4의 N=2 정책은 현행 유지, 어댑터 수정 불요.**

| 구현 | p50 | p95 | 비용 |
|---|---:|---:|---|
| (a) 2병렬 호출 first-ok = min(t₁,t₂) | 40.3s | 45.5s | 2장분 |
| (b) 단일 n=2 호출 | 44.6s | 52.3s | 2장분 |

(a)가 p95에서 **6.8s 빠르고 비용은 같다**(토큰으로 확인: 입력 3777→7555, 출력 1629→3257로
정확히 2배 과금). 단일 호출은 구조적으로 first-ok가 불가능하다 — 두 장이 같은 응답에 실려
**둘 다 끝나야** 반환되므로 min이 아니라 max를 기다린다.
→ **`data[1]`을 읽도록 어댑터를 고칠 이유가 없다.** R5 엔진은 계획대로 병렬 2호출로 간다.

**③ `input_fidelity`는 gpt-image-2 미지원.** 1콜 프로브에서 400 `invalid_input_fidelity_model`:
"The model 'gpt-image-2' does not support the 'input_fidelity' parameter." SDK 시그니처에는
있으나 gpt-image-1 전용이다. **`models.yaml`의 "gpt-image-2는 입력을 항상 high fidelity로
처리" 주석이 맞았다** — 명시할 대상이 아니라 기본 동작이라 명시가 거부된다.
→ c1(로고)·c4(얼굴)의 입력 충실도는 **노브가 아니다.** 이 축은 닫혔고 남은 수단은
입력 해상도(§7.1/§7.2)와 프롬프트뿐이다. R3의 부담이 그만큼 늘었다.
⚠ 혼동 주의: 이건 **R3 축⑤가 아니다.** 축⑤는 *품질 티어(mid vs high)*이며 **여전히 열려 있다** —
승자가 gpt 계열이라 부활한 축이고, 단가 ~3배라 "60초 안에서 품질 최대"의 남은 후보 중 가장 크다.
부수 확인: 이 400이 `refused`가 아니라 `error`로 분류됐다 — 설정 오류가 정책 거부로 둔갑해
게이트 ①을 오염시키는 사고(어댑터 docstring이 경고한 바로 그것)가 실제 400으로 반증됐다.

**④ 60초 계약 재확인 — 통합 풀 n=40 (1152×1536·mid, R1+R2+R2b+R2c+s4a 전량).**
단건 p50 41.4s / p95 **47.6s**. R2의 55.5s는 40건 중 유일한 >50s 표본으로, **이상치 판정이
재확인됐다**(A-2의 주장이 표본 2배로 유지됨).

| N | first-ok p95 | I/O 예산 (60s − p95) | I/O 10s 가정 시 총계 |
|---:|---:|---:|---:|
| 1 | 47.6s | 12.4s | 57.6s ✓ |
| **2 (현행)** | **45.9s** | **14.1s** | **55.9s ✓** |
| 3 | 42.8s | 17.2s | 52.8s ✓ |
| 4 | 42.2s | 17.8s | 52.2s ✓ |

→ **게이트 ② 재유도 판정(A-2)은 유지된다.** 여유는 A-2가 적은 3.8s에서 **4.1s로 소폭 개선**
(first-ok p95가 46.2→45.9s). 다만 **여전히 I/O 10s가 가정이라는 사실은 그대로다** — R5 실측
의무는 해제되지 않는다.
→ **새로 확보된 카드: N=3.** first-ok p95가 45.9→42.8s로 **3.1s 개선**되고 I/O 예산이 17.2s가
된다. 비용은 +$0.08/생성. 912 카드가 죽은 자리를 이것이 대신한다. **N은 시간이 아니라 돈으로
사는 지연 여유**이며(호출이 병렬이라 벽시계가 늘지 않는다), R4에서 정식 검토한다.

**⑤ `gpt_s912` 품질 판정 → §A-7 ①에서 완료. 결론: 미채택, baseline 유지.**
(층 2 승 2 / 패 2 / 무차별 4로 A-5 문턱 미달. 지연 타이브레이커도 순열검정 p=0.675로 무차별.)

**⑥ 관측되었으나 결론으로 삼지 않은 것**: s4b(저해상도 입력)에서 입력 토큰이 3777→3005으로
줄며 `gpt_s912`·`gpt_n2`의 평균 지연이 5~10s 낮아졌다. 그러나 **같은 조건의 `gpt_base`는
오히려 +0.4s**였고 셀당 n=2다 — **부호가 일관되지 않으므로 잡음으로 간주한다.**
"사용자 사진을 축소해 보내면 지연이 준다"는 매력적인 가설이지만 이 데이터로는 주장하지 않는다.
R4에서 셀카 해상도를 축으로 N6로 재실험할 가치는 있다(입력 축소는 계약 변경 없이
엔진 내부에서 가능한 유일한 지연 노브다).

### A-7. S4a/S4b 판정 결과 — **912 미채택, 그리고 R2 채점의 신뢰도 문제**

판정 순서는 지켜졌다: 쌍대 비교 07:30 → s4a 갤러리 07:37 → s4b 갤러리 07:42.
층 2를 먼저 끝냈으므로 **블라인드가 유지된 상태에서 우열 판정이 나왔다.**

#### ① `gpt_s912` — 미채택. **baseline(1152×1536) 유지 확정.**

| 판단 근거 | 결과 |
|---|---|
| 층 2 강제 선택 (8쌍) | **승 2 / 패 2 / 무차별 4** — A-5 채택 문턱(≥6/8) 미달 |
| 층 2 사유 분포 | tie 4 · product 3 · blend 1. 갈린 4쌍이 정확히 2:2 |
| 층 1 회귀 | `gpt_s912`에 **c4 fail 1건**("인물 손 사라짐", 쇼퍼 r2) — 같은 셀에서 baseline은 pass |
| 층 1 완결성 | **백팩 2장 미채점** → A-5가 채택 조건으로 요구한 "회귀 없음"을 **확인할 수 없다** |
| 타이브레이커 ① 지연 | 평균차 +0.8s, **순열검정 p=0.675 → 잡음과 구별되지 않는다.** 무차별 |
| 타이브레이커 ② 단가 | 출력 토큰 1629→1344로 **~13% 저렴** (실재하는 유일한 이점) |
| 타이브레이커 ③ | **baseline 유지** — 근거 없는 변경은 R5 이관 리스크만 늘린다 |

→ **결론: 채택하지 않는다.** 품질은 무차별, 지연 이득은 통계적으로 없고(A-6 ① 재확인),
남는 것은 단가 13%뿐인데 그 대가로 미검증 설정을 R5에 들고 간다. **휴면 카드로 남긴다** —
비용이 실제로 문제가 되면 되살릴 수 있으나, 되살리려면 재검증이 필요하다.
`models.yaml`의 `gpt_s912`는 s4a/s4b 재현성을 위해 `enabled: true`로 둔다.

#### ② ★ 가장 중요한 관측: **c3은 실패한 게 아니라 "판정 불능"이었다.**

채점 대상 24장(ok) 중 **완전 채점 12장 · c3만 공란 10장 · 완전 미채점 2장.**
**공란의 의도는 데이터로 복원되지 않는다.** 갤러리 체크박스가 3상태(값 없음 / true / false)인데
"값 없음"과 "false"가 **화면에서 똑같이 빈 체크박스로 보였기** 때문이다 — 기권한 것인지
fail로 찍었다고 생각한 것인지 구분할 방법이 없다. (이 UI 함정은 아래 ⑦에서 고쳤다.)

다만 어느 쪽으로 읽어도 결론의 방향은 같다:
- **기권으로 읽으면** → cross 12장 중 11장이 fail(2) 또는 판정 불능(9)
- **fail 의도로 읽으면** → cross 12장 중 11장이 fail

그리고 **공란 이미지에는 결함이 note로 상세히 적혀 있다**("스트랩이 팔을 관통..?",
"원래 오른쪽에 감겨야 하는데 왼쪽"). 결함이 관측된 것 자체는 해석과 무관하게 확정이다.
아울러 같은 채점자가 `gpt_base` cross 2장에서는 c3를 **명시적 false로 찍었다** — fail을
기록할 줄 알았다는 뜻이라, 나머지를 비워 둔 데는 이유가 있었을 가능성이 높다.

| wear_position | c3 확정 pass | c3 fail | **c3 공란(기권)** | 계 |
|---|---:|---:|---:|---:|
| `back` (백팩) | 4 | 0 | **0** | 4 |
| `shoulder` (쇼퍼) | 5 | 0 | **1** | 6 |
| `cross` (비세토스) | 1 | 2 | **9** | 12 |

**기권 10건이 전부 스트랩을 지나는 위치(cross·shoulder)에 몰려 있고, back에서는 0건이다.**
이건 우연으로 보기 어렵다. §4의 c3 fail 기준이 `back`의 스트랩 누락만 규정하고 있었으므로,
**규칙이 있는 위치에서는 확신 있게 판정했고 규칙이 없는 위치에서는 기권한 것**이다.

→ 따라서 §4 c3 개정(⑥)은 "일관성을 높이는 일"이 아니라 **"판정을 가능하게 하는 일"**이다.
→ **cross 12장 중 11장이 fail이거나 판정 불능**이다. 이것이 이번 세션의 최대 발견이다.

> 앞선 보고에서 이 공란들을 pass로 집계했다. `report.py`가 부분 채점을 집계에서 제외한
> 덕분에(gpt_base 6장 중 5장만 집계) 드러났다. 위 표가 정확한 값이다.

#### ③ R2의 "c3 100% · 셀 통과 9/9"는 **채점 방법의 산물이다.**

`gpt_base`는 R2 승자 설정에 R2c로 확정된 ref 2장을 얹은 것 — 사실상 **R2 조건의 재현**이다.
그런데 **같은 셀카(s_casual) × 같은 SKU(비세토스 cross)**에서:

| | R2 (구두 일괄 판정) | S4a (이미지별 개별 체크) |
|---|---|---|
| gpt × cross × s_casual | r1 **PASS** (note 없음) · r2 **PASS** (note 없음) | r1 **c3 FAIL** "스트랩이 한 쪽 없는 느낌" · r2 **c3 FAIL** "이전 것보다 더 어색" |

R2의 gpt × cross 6장은 **전부 PASS · note 전무**였다. 결함이 없어서가 아니라
**개별로 보지 않았기 때문**이다. A-1이 "구두 일괄 판정이며 이미지별 개별 체크가 아니다"라고
적어 둔 그 한계가, 이제 **실제로 결론을 바꾸는 크기**임이 확인됐다.

**Go/No-Go 재산정**: cross 셀은 지금 **fail이 아니라 미판정**이다(②). 개정 기준으로 재채점하기
전까지 정확한 값을 낼 수 없고, 범위는 **6/9(cross 3셀 전부 fail) ~ 9/9(전부 pass)**다.
GO 문턱이 ≥6/9이므로 **최악의 경우에도 GO는 유지되지만 여유가 정확히 0이 된다.**
c1은 이번 24장에서 **fail 0건 · 공란 0건** — 제품 정합성(로고·패턴·하드웨어)은 확신 있게
튼튼하다. **흔들리는 곳은 c1이 아니라 c3(착용 위치·스트랩) 하나다.**

> 이 항목은 00-common §7.1/§7.2 기입값을 **바꾸지 않는다.** §7은 *입력* 스펙이고,
> 여기서 드러난 것은 *출력* 결함이다. Dev B의 수집 작업은 영향받지 않는다 — 재통지 불필요.

#### ④ ★ 진짜 결함은 **cross 스트랩**이고, 문서화된 약점(back)과 다르다.

s4a+s4b의 cross(MMRGATA04CO001) **12장 중 11장이 fail(2) 또는 판정 불능(9)**이고,
공란 이미지의 note에는 결함이 구체적으로 적혀 있다. 관측된 결함은 세 갈래다:

| 유형 | 대표 note | pv1이 이걸 막으려 한 문장 |
|---|---|---|
| (A) 스트랩 구간 누락 | "스트랩이 한 쪽 없음" | "The strap must be continuous" |
| (B) 스트랩이 팔을 관통 | "스트랩이 팔을 관통..?" | "pass behind the arm, not floating over it" |
| (C) 착용 위치·좌우 이상 | "원래 오른쪽에 감겨야 하는데 왼쪽" | "from one shoulder to the opposite hip" |

**pv1의 cross 지시문은 세 가지를 전부 명시하고 있고, 셋 다 지켜지지 않았다.**
즉 이건 지시가 없어서가 아니라 **지시가 먹히지 않는** 문제다 — R3에서 문구를 더 쓰는 것으로
풀린다는 보장이 없다.

반면 **back(백팩 MMKGATA03MT001)은 이번 4장에서 지적이 하나도 없었다.** A-3이 "back 스트랩은
레퍼런스로 안 고쳐진다"고 남긴 탓에 back이 최대 약점으로 등재돼 있었는데, **정면 셀카에서
실제로 무너진 것은 back이 아니라 cross였다.** (표본 4장·셀카 1종이므로 "back은 괜찮다"로
뒤집지는 않는다. 다만 **R3의 우선순위는 back → cross로 바꾼다.**)

> **데모 리스크**: 페르소나 **p1(밀라노+데일리+꼬냑)의 확정 SKU가 바로 이 cross 백(No.09)**이다
> (00-common §9). 간판 페르소나가 가장 약한 wear_position 위에 서 있다. R6가 N6~8 best-of로
> 건질 수는 있으나, R3에서 못 고치면 p1 자산 확보가 가장 비싼 작업이 된다.

#### ⑤ 잔여 불일치 — c3/c4 귀속이 흔들린다

기권(②)과 별개로, **같은 결함을 다른 항목에 귀속시킨** 사례가 있다. 둘 다 같은 SKU·같은 rep다:

| 이미지 | note | 채점 |
|---|---|---|
| `gpt_base` × 쇼퍼 r2 | "합성 과정에서 **팔이 사라짐**" | c3 **공란**, c4 **pass** |
| `gpt_s912` × 쇼퍼 r2 | "인물 **손 사라짐**" | c4 **fail** |

더 심한 결함(팔 소실)이 c4 pass고, 덜한 결함(손 소실)이 c4 fail이다.
→ **`gpt_s912`의 c4 fail 1건을 "s912의 회귀"로 읽으면 안 된다.** 귀속 흔들림의 산물일 수 있다.
①의 미채택 결론은 이 fail에 기대지 않는다 — 층 2 무차별과 지연 이득 부재만으로 성립한다.

이 문제는 R3의 전제를 위협한다: R3는 축별 1:1 비교인데, 판정 규칙이 흔들리면
**두 팔의 차이가 아니라 채점 잡음을 측정하게 된다.** 그래서 ⑦의 개정이 R3의 선행 조건이다.

#### ⑥ [음성 결과] 저해상도 입력은 c1·c4를 무너뜨리지 않았다 — **§7.2는 그대로 둔다.**

s4b(380×1244, 얼굴 폭 ~45px) 6장에서 **c1·c2·c4·c5·c6 전부 pass — fail 0건 · 공란 0건.**
(c3만 6장 전부 공란이며, 그건 해상도가 아니라 ②의 규칙 부재 때문이다.)
가설은 "긴 변 1244px로 §7.2를 통과하지만 폭 380px이라 로고·얼굴이 무너진다"였고, **반증됐다.**
→ §7.2의 "최소 해상도 긴 변 1024px"를 **강화할 근거가 없다. 개정하지 않는다.**
→ `prep_assets.MIN_SHORT_EDGE_WARN = 768`은 **가설로만 남는다** — 코드에 경고로 두는 것은
무해하니 유지하되, §7.2로 승격할 근거는 아니다.
> 단 s4b의 note는 전부 c3(스트랩·위치) 지적이었다. 저해상도 셀에서도 **같은 cross 결함이
> 재현**됐다는 뜻이며, 이는 ④를 입력 품질과 무관한 **모델의 구조적 한계** 쪽으로 밀어 준다.

#### ⑦ 조치: §4 c3 fail 기준 개정 (사유 기록 의무 이행)

§4 c3의 fail 기준에 cross·shoulder의 스트랩 연속성을 명문화했다. **이 개정은 R2와의 비교
가능성을 끊는다** — R2는 (a) 구두 일괄 판정이었고 (b) 개정 전 기준으로 채점됐다.
따라서 **R2의 c3 통과율과 R3 이후의 c3 통과율을 나란히 놓지 말 것.**
(report.py는 손대지 않았다 — c3 판정은 사람이 하고 도구는 집계만 한다.)

#### ⑧ 조치: 갤러리가 "미판정"을 보이게 고쳤다 (`gallery.py`)

②의 유실은 **도구 결함**이다. 체크박스 1클릭=pass / 2클릭=fail / 0클릭=미판정인데,
fail과 미판정이 화면상 동일했다. 재채점에서 같은 일이 반복된다.

- 미판정 항목은 **호박색 + `?`**, fail은 **빨강 취소선**으로 구분 표시
- 일부만 찍힌 카드는 **테두리 호박색 + "⚠ 미판정 c3 — 집계에서 제외된다"** 배지
- 헤더에 **"일부만 판정된 이미지 N건"** 카운터 (report가 조용히 빼는 것을 채점 중에 알린다)

데이터 모델은 그대로라 **기존 채점은 보존된다**(localStorage 키 불변). 갤러리 2개 재생성 완료.

#### ⑨ 남은 작업

1. **[필수·R3 선행] c3 공란 10장 재채점** — 개정된 기준(ⓐ스트랩 구간 누락 ⓑ팔·몸통 관통
   ⓒ좌우 반전)으로 다시 본다. 갤러리 툴팁·미판정 표시는 이미 동기화돼 있다.
   이걸 해야 ②의 Go/No-Go가 추정 범위(6/9~9/9)에서 **실측치**가 되고, R3 축⑥의
   **개선 여부를 잴 기준선**이 생긴다. 기준선 없이 R3를 돌리면 개선을 증명할 수 없다.
2. **[선택] 백팩 2장 미채점**(`gpt_s912 × MMKGATA03MT001 × r1,r2`) — 채우면 ①의
   "회귀 없음"이 확정된다. 결론(baseline 유지)은 바뀌지 않는다.

### A-8. c3 재채점 처리 기록 ~~+ cross 붕괴 실측(8%)~~ **→ 수치는 A-9 ①로 대체**

> ⚠ **이 절의 cross 수치(1/12 = 8%)와 그에 딸린 데모 리스크 표는 A-9 ①이 대체한다.**
> S4a/S4b가 cross 실패 자세(정면·직립) 셀카만 써서 생긴 표본 편향이었다.
> 재채점 처리 내역과 §7.1/§7.2 관련 기록은 유효하다.

A-7 ②의 공란 10건을 **Dev A가 전부 `fail`로 확정**(2026-08-17). 개정 c3 기준 적용.
10건 전부 note를 보유해 §4의 note 의무는 충족. 원본은 `scores.pre_c3.json`으로 보존했고,
`scorer` 필드에 **"갤러리 클릭이 아니라 전사"**임을 기록했다(A-1과 같은 감사 규율).
완전 미채점 2장(`gpt_s912 × 백팩`)은 **손대지 않았다** — 한 번도 열어보지 않은 이미지에
판정을 대입할 수 없다.

#### 확정 수치 — 결함이 **c3 · cross 한 칸에만** 몰려 있다

| wear_position | c3 pass | **전항목 pass** | 표본 |
|---|---:|---:|---:|
| `back` (백팩) | 4/4 **100%** | 4/4 **100%** | 4 |
| `shoulder` (쇼퍼) | 5/6 83% | 4/6 67% | 6 |
| `cross` (비세토스) | **1/12 8%** | **1/12 8%** | 12 |

**c1·c2·c4·c5·c6은 전 조건에서 100%다**(c4만 s912에서 1건 fail — A-7 ⑤의 귀속 흔들림).
즉 이 엔진은 **제품도 인물도 조명도 다 맞히는데 크로스바디 스트랩 하나를 못 그린다.**

팔·셀카를 갈라도 결론이 같다 — 설정이나 입력의 문제가 아니다:

| 갈래 | cross 전항목 pass |
|---|---:|
| `gpt_base` (= R5 이관 확정 설정) | **0/4** |
| `gpt_n2` | 0/4 |
| `gpt_s912` | 1/4 |
| s_casual (표준 셀카) | 1/6 |
| s_smart_lowres (난도 셀카) | 0/6 |

#### 데모 리스크 — R4 산식을 앞당겨 적용

05 §3 R4의 `라이브 N=2 성공률 = 1-(1-p)^N`에 실측 p를 넣는다:

| wear_position | p (장당) | **라이브 N=2** | R6 N=8 |
|---|---:|---:|---:|
| `back` | 1.00 (4/4) | ~100% | ~100% |
| `shoulder` | 0.67 (4/6) | 89% | ~100% |
| **`cross`** | **0.083 (1/12)** | **16%** | **50%** |

> ⚠ n=4~12다. **정밀도가 아니라 자릿수를 읽어라** — cross가 "가끔 실패"가 아니라
> "가끔 성공"이라는 것이 요지다. 05 §9대로 유의성은 따지지 않는다.

**이것이 뜻하는 바 두 가지:**
1. **라이브 데모에서 사용자가 크로스바디를 뽑으면 84% 실패한다.** 00-common §8 STEP 2의
   `outing`(#외출·파티)이 "콤팩트 포인트(크로스바디·미니)"로 매핑되므로, **4갈래 중 한 갈래가
   통째로 깨진 경험**이 된다.
2. **간판 페르소나 p1의 확정 SKU가 그 크로스바디(No.09)다.** R6에서 N=8을 돌려도
   쓸 만한 자산 1장을 얻을 확률이 **동전 던지기(50%)**다.

#### Go/No-Go 재산정

`gpt_base × s_casual`의 셀 통과가 **2/3**(back·shoulder pass, cross fail)이고,
cross는 **서로 다른 셀카 2종에서 모두 실패**했다. R2의 9셀로 환산하면 **6/9**가 최선 추정이다.

- 05 §5 GO 기준 = 셀 통과 ≥6/9 **AND** c1 ≥70%. c1은 100%. → **GO는 유지된다. 단 문턱에 정확히 걸친다.**
- 그러나 **실질적으로는 CONDITIONAL GO의 상황**이다. §5가 CONDITIONAL GO에 붙인 행동이
  **"약한 wear_position 제외 + §7.2 사진 조건 강화 + 제한 내용을 데모 시나리오에 반영"**인데,
  지금 필요한 조치가 정확히 그것이다. **숫자는 GO, 처방은 CONDITIONAL GO** — 라벨보다 처방을 따른다.
- NO-GO 조항("그럴듯한 가짜 제품이 구조적")은 **해당 없다**. c1이 100%라 가짜 제품 문제가 아니다.

#### 결정 트리 (순서 고정 — 건너뛰지 말 것)

1. **[무료·즉시] R2의 gpt × cross 6장을 개정 기준으로 재채점.** 이미지는
   `experiments/r2_bakeoff/images/`에 그대로 있다. 표본이 12→18로 늘고 **셀카 3종
   (s_casual·s_smart·s_formal)을 덮어** Go/No-Go가 추정치에서 **실측치**가 된다. $0.
2. **R3 축⑥ (cross 스트랩 지시)** — 유일한 정면 돌파. 단 pv1이 이미 세 조건을 명시하고도
   실패했으므로 성공을 전제하지 않는다.
3. 축⑥ 실패 시, 순서대로:
   (i) §7.2에 "정면 직립 셀카 + cross는 비권장" 추가 → 데모 시나리오에서 회피
   (ii) R6를 N=8이 아니라 **N=16 이상**으로 물량 확보 (p=0.083이면 N=16에서 75%)
   (iii) **p1의 SKU를 back/shoulder 계열로 교체 — Dev B 협의 필요**
4. **B 통지는 3-(iii)에 도달하기 전까지 하지 않는다.** 00-common §3("생성이 막혀도 B를
   끌어들이지 않는다")대로 A 쪽에서 줄이는 수단을 먼저 소진한다. 다만 (iii)는 `matchable`
   플래그와 페르소나 표를 건드리므로 **스카프 미결② 해소와 같은 급의 통지**가 된다.

### A-9. R2 전면 재채점 — **A-8의 8%는 표본 편향이었다. 두 결함이 분리된다.**

R2 36장을 개정 기준으로 재채점하자 **A-8의 결론 하나가 무너지고 하나가 정밀해졌다.**
A-8은 S4a/S4b(gpt 계열 완전채점 22장)만 봤는데, 그 두 실험이 **하필 cross가 실패하는
셀카 2종만** 쓰고 있었다. R2를 합쳐 gpt 계열 40장으로 보면 그림이 다르다.

#### ① [정정] cross는 8%가 아니라 28%다 — 그리고 **셀카가 가른다**

| 셀카 | cross 전항목 pass | 조건 |
|---|---:|---|
| `s_formal` (허벅지 위 크롭) | **2/2** | R2 |
| `s_smart` (팔짱·측면) | **2/2** | R2 |
| `s_casual` (정면·직립·팔 내림) | **1/8** | R2 + S4a |
| `s_smart_lowres` (정면·직립·저해상도) | **0/6** | S4b |
| **합계** | **5/18 = 28%** | |

**"cross가 깨졌다"가 아니라 "정면·직립·팔을 몸 옆에 내린 자세에서 cross가 깨진다"**가 정확한 진술이다.
A-8이 8%를 낸 것은 S4a·S4b가 그 자세 2종만 썼기 때문이다. **A-8 ①의 표와 데모 리스크 수치는
이 절로 대체한다.**

**결함의 정체가 note로 특정됐다 — 두 모델이 똑같이 실패한다:**
- gpt: "팔과 몸통 **사이에서 끝나야** 어색하지 않음" / "팔과 몸통의 **경계를 잘못 본** 듯"
- gemini: "**팔과 몸 사이에 공간이 있으면 뒤에 스트랩이 살짝 보여야** 자연스러운데, 이 표현이 없음"

→ **팔과 몸통 사이에 틈이 생기는 자세에서, 그 틈으로 보여야 할 스트랩 구간을 렌더링하지 못한다.**
팔짱(s_smart)이나 크롭(s_formal)으로 그 틈이 없으면 문제가 사라진다.
이건 R3 축⑥이 겨눌 과녁이 "스트랩을 그려라"가 아니라 **"팔–몸통 틈으로 스트랩을 통과시켜라"**임을 뜻한다.

#### ② [신규] `back` 실패는 모델이 아니라 **레퍼런스 부족**이었다

| ref 장수 | back 전항목 pass | 출처 |
|---|---:|---|
| **1장** (정면 팩샷만) | **1/6** | R2 |
| **2장** (+후면 등판컷) | **4/4** | S4a |

R2 back 실패 5건의 note가 **전부 동일**하다: *"스트랩 색이 다름. 스트랩은 그냥 검은색인데.
근데 이건 사진에 100% 안 나와서 그려낸 걸수도."* — 채점자가 원인까지 짚었다.
**정면 팩샷에는 어깨 스트랩이 보이지 않으므로 모델이 색을 지어낸다.** 후면 등판컷을 주면 사라진다.

> **A-3 #3과 모순되지 않는다.** A-3은 "back 스트랩은 레퍼런스로 해결되지 않는다"고 했는데
> 그건 **c3(스트랩의 존재·기하)** 이야기였고, 이건 **c1(스트랩의 색)** 이다. 서로 다른 결함이
> 같은 단어("back 스트랩")에 묶여 있었다. 레퍼런스는 **색은 고치고 기하는 못 고친다.**

→ **00-common §7.1 개정 대상**: `back` 카테고리는 보조컷이 "권장"이 아니라 **사실상 필수**다.
→ 05 §3 R4의 "back 엣지 → §7.2에 반측면 권장 반영" 계획은 **과녁이 틀렸다.** §7.2(사용자 사진)가
   아니라 §7.1(상품 레퍼런스)이 답이었다.

#### ③ Go/No-Go 확정 및 전망

- **기록값: gpt 셀 통과 6/9** (실패 셀 = s_casual×back, s_smart×back, s_casual×cross).
  GO 문턱(≥6/9)에 **정확히 걸친다**. c1 83% ≥ 70% → **GO 유지.**
- **전망: R5 배포 설정(ref 2장)에서 8/9.** 실패 3셀 중 2셀이 back이고 ②로 해소되기 때문이다.
  남는 실패는 `s_casual × cross` 하나다.
- ⚠ **c1 민감도 경고**: back 실패 5건 중 **3건이 c2로, 2건이 c1으로** 기록됐는데 note는 전부
  동일한 "스트랩 색" 문제다. 색 이탈은 §4상 **c1**이다. 3건을 c1으로 재귀속하면
  **c1 = 12/18 = 67% < 70% → GO 기준 미달(CONDITIONAL GO)**이 된다.
  다만 ②로 이 결함 자체가 사라지므로 **배포 설정에서는 쟁점이 소멸한다.** 재귀속은 하지 않되
  **"c1이 70% 문턱에서 6%p 안쪽"이었다는 사실은 기록한다.**

#### ④ 위치별 라이브 성공률 (gpt 40장, R4 산식 선행 적용)

| wear_position | p (장당) | 라이브 N=2 | 비고 |
|---|---:|---:|---|
| `shoulder` | 0.75 (9/12) | **94%** | 안전 |
| `back` | 0.50 (5/10) | 75% | **ref 2장이면 4/4 → 실질 ~100%** |
| `cross` | 0.28 (5/18) | 48% | **자세 의존** — 정면·직립에서만 실패 |

> n=10~18. 자릿수로만 읽어라(05 §9).

### A-10. R3 축⑥ — cross 스트랩 지시 (`pv1c`) **기각**

`r3f_cross`: gpt_base × {s_casual, s_smart} × cross × {pv1, pv1c} × N4 = 16장, $1.28, 16/16 ok.
`pv1c`는 `pv1`과 **cross 지시 한 문단만** 다르다(다른 지시·템플릿 본문은 바이트 단위 동일 검증).
판정선은 `configs/r3f_cross.yaml` 헤더에 **실행 전 확정**.

#### ① 판정 — 사전 확정선 **2개 모두 위반**

| 셀카 | pv1 | pv1c |
|---|---:|---:|
| s_casual (실패 조건) | **1/4** | **0/4** |
| s_smart (회귀 대조군) | **4/4** | **2/4** |

① `s_casual에서 pv1c c3 pass ≥ 2/4` → **0/4** ✗ ② `s_smart 회귀 없음` → **4/4 → 2/4** ✗
⇒ **기각. `pv1` 유지.**

> **채점 방식 한계**: 갤러리 개별 체크가 아니라 **구두 판정**이며 **c3만** 판정됐다.
> 축⑥의 판정선이 c3 단일 지표였으므로 판정은 성립하지만, **이 실험의 셀 통과·전항목 pass는
> 계산하지 않는다.** `scores.json`의 `scorer`에 명시했다.

**층 1과 층 2가 갈렸고, 그게 2층 설계의 존재 이유다.** 층 2(강제 선택)에서 pv1c는 s_casual
3/4로 이겼는데 **사유가 전부 `blend`(조명·융합)**였다 — 보기에는 나아졌지만 겨눈 c3는 하나도
못 고쳤다. 층 2만 있었다면 채택으로 오판했을 것이다.

#### ② ★ pv1c가 **좌우(laterality)를 새로 망가뜨렸다** — pv2 규칙

`s_smart × pv1c` 4장 중 **2장(r1·r3)에서 가방과 스트랩이 같은 쪽**이다. 스트랩이 몸통을
가로지르지 않고 세로로 흐른다 = 크로스바디가 아니라 **숄더백**이다. `pv1`에는 없다(4/4 정상).

| 버전 | 대각선을 지정하는 부분 |
|---|---|
| pv1 | *"the strap runs diagonally across the torso **from one shoulder to the opposite hip**"* |
| pv1c | *"running **from the near shoulder**, diagonally across the chest, down to the opposite hip"* |

**`near shoulder`에는 기준점이 없다.** 가림 판정 규칙을 넣으면서 **대각선을 못 박고 있던
유일한 문장을 흐려 놓았다.** 축⑥은 c3를 고치려다 c3의 다른 축을 깨뜨렸다.

**pv2 규칙 (확정)**: cross 지시를 어떻게 다시 쓰든 `one shoulder → opposite hip`이라는
**상대 지시 형태를 보존한다.** 절대 좌우(left/right)도, 기준점 없는 상대어(near/far)도 쓰지 않는다.
**인물의 좌우는 입력 사진마다 달라 프롬프트가 알 수 없다.**

#### ③ 설계 교훈 — 변경 최소 단위를 **문장**으로

축⑥은 지시 **한 문단**을 통째로 갈았고 그 안에 (a) 가림 규칙 추가와 (b) 대각선 문구 약화가
함께 들어 있었다. 결과가 나빠도 둘 중 무엇 때문인지 가릴 수 없다.
→ **(a)는 아직 기각되지 않았다.** (b)에 오염돼 함께 죽었을 뿐이다.
→ R3의 남은 축에서는 **문단이 아니라 문장 단위로** 가른다.

#### ④ [철회] "두 번째 레퍼런스가 cross를 해친다" 가설

초안에서 `s_smart × cross`가 R2(ref1) 2/2 → r3f(ref2) 0/4로 뒤집힌 것을 근거로 세운 가설이었다.
**그 0/4는 채점 전사 오류였다** — "예외 1장 빼고 전부 어색"이라는 진술은 **s_casual 8장에
대한 것**인데 16장 전체로 넓혀 읽었다. 정정하면 **r3f · s_smart × cross = 4/4로 회귀가 없다.**
보조컷은 무죄이며 §7.1은 손대지 않는다.

#### ⑤ 정정 후 A-9 ①이 **강화된다** — cross는 자세로 갈린다

| 셀카 | 자세 | c3 pass |
|---|---|---:|
| `s_smart` | 팔짱 — 팔·몸통 틈 없음 | **6/6** |
| `s_formal` | 허벅지 위 크롭 | **2/2** |
| `s_casual` | 정면·직립·팔 내림 — **틈 있음** | **1/10** |
| `s_smart_lowres` | 정면·직립 — **틈 있음** | **0/6** |

**틈 있는 자세 1/16, 없는 자세 8/8.** "cross가 깨졌다"가 아니라
**"팔–몸통 틈이 보이는 자세에서만 깨진다"**는 국소적 결함이다.
→ **A-8 결정 트리에서 (i) §7.2 자세 조건이 1순위로 확정된다.** SKU 교체는 한참 뒤로 밀린다.
→ 다만 **실사용자 자세는 통제 못 한다.** §7.2는 권고이지 강제가 아니다.
   자세를 우리가 고르는 R6 데모 자산은 안전하다.

#### ⑥ 정지 규칙 (사전 확정)

cross 결함에 대한 **프롬프트 시도는 `pv1d` 한 번으로 끝낸다.** 실패하면 프롬프트 축을 닫고
§7.2 자세 조건으로 간다. 근거: pv1은 연속성·팔 뒤 통과·대각선을 **이미 전부 명시**하고도
실패했고, **gpt와 gemini가 같은 방식으로 실패**한다(§A-9 ①) — 지시 부족이 아니라 **모델
능력의 한계**라는 신호다. 문구를 계속 바꾸는 것은 05 §9가 경계하는 방향타 없는 튜닝이다.

---

### A-11. ★ 사고 기록 — **자기 회선 열화를 프로바이더 지연으로 오인**

`r3g`/`r3h` 실행이 무너진 뒤(timeout 7 · `APIConnectionError` 5) 원인을 갈랐다.
**이미지 생성이 아닌 경로**를 재 보니 회선 자체가 죽어 있었다:

| 측정 대상 | 정상 | 실측 |
|---|---|---|
| `google.com/generate_204` (본문 없는 204) | 20~50ms | **2,535 / 7,550 / 12,689 ms** |
| `api.openai.com/v1/models` (단순 GET) | ~200ms | 2,315 / 6,459 / 6,959 ms |
| `models.retrieve` (무과금 제어면) | ~300ms | 1,184 / 4,179 / 8,922 ms |

**본문이 거의 없는 요청이 초 단위 = 대역폭이 아니라 회선 자체의 문제다.**
어댑터의 `latency_ms`는 **입력 ~600KB 업로드 + 결과 ~300KB 다운로드를 포함한 벽시계**이므로,
이 상태의 측정치는 생성 시간이 아니라 **우리 회선 상태**를 잰 것이다.

**철회한 주장 2개** (이 세션에서 한때 부록에 기록했던 것):
1. ~~"프로바이더 지연이 세션 간 +23% 이동했다"(r3f p50 50.9s)~~ — 근거 없음.
   r3f도 같은 열화의 초기 국면이었을 가능성이 높으나, 당시 네트워크 기준선을 재지 않아 확정 불가.
2. ~~"N을 올려도 60초 계약을 못 지킨다"~~ — 계산의 입력 분포가 오염돼 있었다.

→ **유효한 지연 기준선은 여전히 n=40 풀뿐이다**(§A-6 ④): p50 41.4s · N=2 first-ok p95 45.9s ·
I/O 예산 14.1s. **60초 계약의 위험도는 A-6 ④ 상태 — 얇지만 통과.**
→ **오염되지 않은 것**: 이미지 자체. 느린 회선은 페이로드를 늦게 나를 뿐 내용을 바꾸지 않는다.
   **성공분은 품질(c3) 판정에 그대로 쓸 수 있다.**

**도구 보강 (`runner.probe_network`)**: 매 실행 전(`--fake` 제외) 무과금 204 경로 RTT를 재고
**800ms 초과 시 경고**한다. **차단하지 않는다** — 판단은 사람이 한다.
이게 있었다면 이 세션에서 $2.64와 결론 2개를 날리지 않았다.
§7 리스크 레지스터의 "프로바이더 장애" 항목은 **자기 회선과 구별할 수단이 있어야** 성립한다.

---

### A-12. 미완 상태 — ~~회선 회복 후 재개할 것~~ **→ §A-13이 대체 (재개 안 함)**

> ⚠ **이 절은 §A-13이 대체한다 (2026-08-18).** 두 실험 모두 **재개하지 않는다** — `r3g`는
> **미판정-기각**으로 정지 규칙이 발동했고(§A-13 ⑤), `r3h`의 축⑤는 **폐기**됐다(§A-13 ⑥).
> 아래 재개 절차·견적·판정선 경고는 **역사적 기록**으로만 남긴다. 다만 마지막 문단의
> "분모 문제" 경고는 §A-13 ⑤의 판정 근거가 됐으므로 반드시 함께 읽을 것.

두 실험이 회선 열화(A-11) 중에 실행되어 **분량이 미달**이다. 이미지는 유효하나 표본이 얇다.

| 실험 | 축 | 현재 | 계획 | 남은 견적 |
|---|---|---|---|---:|
| `r3g_cross_pv1d` | pv1 vs **pv1d** (프롬프트 마지막 시도) | s_casual 2v2 · s_smart 3v3 | 4v4 · 4v4 | **$0.48** |
| `r3h_quality` | mid vs **high** (축⑤) | gpt_base 4 · **gpt_high 1** | 4 · 4 | **$0.60** |

**재개 조건**: `probe_network`가 **100ms 이하**를 보고할 때. (기록 시점 435ms — 정상의 10배.)
그 전에 돌리면 timeout이 섞이고, 러너는 **실패 시도에도 단가를 더하므로**(보수적 설계)
예산만 샌다 — 이 세션 견적 $2.40 대비 실제 $2.64가 나간 이유다.

```powershell
python -m genlab run configs/r3g_cross_pv1d.yaml --resume   # 캡 조정 불요
python -m genlab run configs/r3h_quality.yaml   --resume     # 캡 조정 불요
```

⚠ **판정선의 분모 문제**: `r3g`의 사전 확정선은 `s_casual에서 pv1d c3 pass ≥ 2/4`다.
지금 분모가 2라 **2/2가 나와도 판정선을 충족했다고 말할 수 없다.** 반드시 4v4를 채운 뒤 판정한다.
`r3h`의 `gpt_high`는 n=1이라 축⑤ 판정 자체가 불가능하다.

⚠ **`r3h` 지연 게이트**: `gpt_high` 성공 1건이 114.3s였으나 **회선 오염분이라 무의미하다.**
재개 후 깨끗한 측정으로 다시 본다. 판정선은 config 헤더대로 **품질 AND 지연** 둘 다 넘겨야 채택이며,
품질만 이기면 **R6 데모 자산 전용 격리 채택**을 검토한다(R6는 사전 생성이라 60초 계약 무관).

---

### A-13. ★ 2026-08-18 실측 — **60초 계약이 실행 단위로 붕괴했고, R3를 종결했다**

`r3g_cross_pv1d`(18시도) + `r3h_quality`(9시도) = **27시도 · $2.64.**
이 두 실행은 §A-11이 회선 열화를 적발한 **바로 그 세션**이다. A-11은 사고의 *발견*을 적었고,
이 절은 그 세션이 **남긴 데이터 전량**을 집계해 결론으로 확정한다.
지금까지 이 수치는 두 `manifest.jsonl`에만 있었고 부록에 기록된 적이 없다.

#### ① 먼저 §A-11과의 관계를 못 박는다 — **원인은 미해결, 운영 결론은 원인에 의존하지 않는다**

A-11은 이 세션의 **지연 데이터를 폐기**하고 품질 데이터만 채택하라고 적었다.
**그 규율은 유효하며 이 절이 뒤집지 않는다:**

- **깨끗한 지연 기준선은 여전히 `n=40` 통합 풀뿐이다**(§A-6 ④ — p50 41.4s · N=2 first-ok p95 45.9s).
  아래 ②~④의 수치는 **그 기준선을 갱신하지 않는다.**
- 아래를 **"프로바이더가 느려졌다"로 읽지 말 것.** 어댑터의 `latency_ms`는 업로드 ~600KB +
  생성 + 다운로드 ~300KB를 포함한 **벽시계**이므로, 회선이 죽은 상태의 측정치는 회선을 잰 것이다.
  같은 오독으로 이미 결론 2개를 철회했다(§A-11).

**그럼에도 이 데이터를 기록으로 승격하는 이유**: 우리가 답해야 할 질문이 *"누구 탓인가"*가 아니라
*"발표장에서 60초 안에 끝나는가"*이기 때문이다. **두 질문의 답은 다르다.**
원인이 프로바이더든 우리 회선이든 **사용자에게는 똑같이 60초 초과**이고, 발표장 네트워크가
사무실 회선보다 나을 이유는 없다 — 공용 Wi-Fi·수백 명 동시 접속·테더링이 전형이다.

→ **이 세션은 "정상 지연 측정치"로는 무효, "열화 시나리오의 실측 리허설"로는 유효하다.**
   그 자격으로만 인용한다.

#### ② 실측 — `gpt_base`(mid)는 이 세션에서 **60초 안에 한 번도 끝나지 않았다**

| 항목 | 값 |
|---|---|
| `gpt_base` 시도 | **23** (r3g 18 + r3h 5) |
| ok | 14 |
| **60초 이내 ok** | **0 / 23** |
| ok 지연 범위 | **61.9s ~ 97.9s** — **최속조차 계약선을 1.9s 넘는다** |
| timeout | 6 (러너 하드컷 95s = `timeout_s: 90` + 5s) |
| `APIConnectionError` | 3 |

두 실험 **합계**(`gpt_high` 포함)로는 **timeout 7 · `APIConnectionError` 5**이며, §A-11이 적은
수치와 정확히 일치한다. 출처: `experiments/r3g_cross_pv1d/manifest.jsonl` ·
`experiments/r3h_quality/manifest.jsonl`.

> ⚠ 표본은 셀카 2종 × cross SKU 1종에 한정된다. 05 §9대로 **자릿수로 읽어라** — 요지는
> "느려졌다"는 정도 문제가 아니라 **"성공한 것조차 전부 계약선 밖이었다"**는 이진 사실이다.

#### ③ **N 병렬로는 구제되지 않는다** — 이게 이 절의 핵심이다

N=k 병렬 first-ok의 하한은 **그 세션의 최속 1건**이다. 오늘의 최속은 **61.9s**로 이미 60s 밖이다.
→ **N을 아무리 올려도 이 상태에서는 계약을 충족할 수 없다.** §A-6 ④가 확보한 "N=3 카드"
(first-ok p95 42.8s)는 **정상 회선에서만 유효한 카드**이고, 열화 시나리오에서는 **0원짜리다.**
→ 지연에 대한 방어는 N도 여유 초도 아니다. **폴백이 유일한 수단이다.** (B 통지 (a).)

#### ④ p50 추이 — 세 세션

| 세션 | p50 | 성격 |
|---|---:|---|
| 통합 풀 n=40 (R1~S4a) | **41.4s** | 깨끗한 기준선 (§A-6 ④) |
| `r3f` (08-17) | **50.9s** | 회선 열화 초기 국면 추정 — §A-11이 "+23% 드리프트" 주장을 **철회**한 구간 |
| `r3g`+`r3h` (08-18) | **80.8s** | 본 절 (nearest-rank p50 79.7s) |

**이 표를 "프로바이더 드리프트"의 증거로 쓰지 말 것** — §A-11이 철회한 바로 그 오류다.
읽는 법은 하나뿐이다: **회선이 나빠지면 벽시계 지연은 2배로 간다.**

#### ⑤ `pv1d` — **"미판정-기각"**. 정지 규칙 발동, cross 프롬프트 축 **공식 종결**

`r3g`의 사전 확정 판정선(`configs/r3g_cross_pv1d.yaml` 헤더)은
**`s_casual`에서 `pv1d` c3 pass ≥ 2/4 AND `s_smart` 4/4 유지**였다.
timeout·conn error로 **양 팔 모두 표본이 결손**됐다:

| 셀카 | `pv1` (대조) | `pv1d` (변형) | 계획 |
|---|---:|---:|---:|
| `s_casual` (실패 조건) | 2 도달 | **2 도달** | 4 |
| `s_smart` (회귀 대조군) | 3 도달 | **3 도달** | 4 |

분모가 4가 아니므로 **"2/4 이상"도 "4/4 유지"도 성립 여부를 말할 수 없다.**
§A-12가 미리 경고한 그대로다(*"2/2가 나와도 판정선을 충족했다고 말할 수 없다"*).

→ **판정 = 미판정.** 그리고 사전 확정된 정지 규칙(§A-10 ⑥)은 **"기각이면 종료"**이며,
**미판정은 채택이 아니다.** 채택 사유를 만들지 못한 이상 규칙은 발동한다.
사전등록에 attrition(표본 결손) 규칙이 없었던 것은 설계 결함이며, 그 결함을 **사후에
분모를 바꿔** 메우는 것이 §A-5가 명시적으로 금지한 행위다.

> **왜 재실행하지 않는가**
> (a) 정지 규칙의 근거는 표본 크기가 아니라 **모델 능력의 한계라는 신호**였다 — `pv1`이 연속성·
>     팔 뒤 통과·대각선을 **전부 명시하고도** 실패했고 **두 벤더가 같은 방식으로** 실패한다(§A-9 ①).
>     표본을 채워도 그 신호는 변하지 않는다.
> (b) 남은 시간은 D4이고 **R6 데모 자산이 미착수**다. $0.48을 아끼는 문제가 아니라 시간 배분 문제다.
> (c) §A-10 ⑤가 이미 **§7.2 자세 조건을 1순위 대안으로 확정**했고, 자세를 우리가 고르는
>     R6 자산은 그 경로로 안전하다.
> → **프롬프트로 cross를 고치려는 시도는 여기서 끝난다. 문구를 더 바꾸지 않는다**(05 §9).

> **[선택·저비용 대체 경로]** `r3g`의 ok 10장은 회선과 무관하게 **유효한 이미지**다(§A-11).
> 시간이 나면 **갤러리 개별 채점 15분**으로 `pv1` vs `pv1d`의 c3를 10장 한정 관측할 수 있다.
> 단 이것은 **사전 판정선을 충족하는 판정이 아니라 참고 관측**이며,
> **결과가 무엇이든 위 종결은 되돌리지 않는다.**

#### ⑥ 축⑤ (품질 티어 mid vs high) — **폐기**

`gpt_high` 4시도 → ok 1건 **114.3s**, conn error 2, timeout 1(하드컷 125s).

- `r3h`의 사전 판정선은 **품질 AND 지연**이고 지연 게이트는 **N=2 first-ok + I/O 10s ≤ 60s**다.
  유일한 성공이 **114.3s**면 **품질을 보기도 전에 지연에서 탈락**한다. n=1이라 품질 판정도 불가능하다.
- ⚠ 이 114.3s 역시 회선 오염분이라 **"high는 114초짜리다"라고 주장하지 않는다.** 주장하는 것은
  **"이 축을 판정할 데이터를 얻지 못했고, 다시 얻을 시간이 없다"**는 것뿐이다.
- 잔여 카드였던 **"R6 데모 자산 전용 격리 채택"**(사전 생성이라 60초 계약 무관)도 **쓰지 않는다**:
  R6가 겨눈 결함은 cross 스트랩 기하인데 **high가 그것을 고친다는 관측이 한 장도 없다.**
  단가 2.5배와 미검증 설정을 R6에 들이는 대가로 얻는 것이 없다(§A-5 타이브레이커 ③과 같은 규율).
- → **R6는 `gpt_base`로 간다. 축⑤는 닫혔다.**

#### ⑦ 산출물 ④ **지상 동결** — pv2 = pv1 (변경 0)

05 §1의 산출물 ④("확정 프롬프트 pv2 + N/재시도/타임아웃 정책 = 실엔진 구현 스펙")를 동결한다.

| 항목 | 동결값 | 근거 |
|---|---|---|
| 프롬프트 | **pv2 = pv1. diff 없음** | 축⑥ 기각·종결(§A-10 ①, 본 절 ⑤). 축②③④ 미착수 종료 |
| 모델 설정 | **`gpt_base`** = `gpt-image-2` · quality=mid · 1152×1536 · ref 2장 | §A-6 ①③ · §A-7 ① · 본 절 ⑥ |
| N | **N=2 병렬 first-ok** (단일 `n=2` 호출 아님 — 구조적으로 min이 불가능) | §A-6 ② |
| 재시도 | network·timeout **1회** / refused **프롬프트 순화 변형 1회** | 05 §3 R4 |
| 엔진 타임아웃 | 다운로드 5s + 생성 40s + 업로드 5s + 여유 5s = **55s** (`engines/base.py`와 정합) | 05 §3 R4 |
| **⚠ 계약 준수 조건** | **60초 준수는 회선·프로바이더 상태에 의존한다.** 정상 회선: N=2 first-ok p95 45.9s + I/O 10s = 55.9s **충족**(§A-6 ④). 열화 시: **0/23**(본 절 ②) | |

**"변경 0"은 실패가 아니라 결론이다.** R3는 프롬프트를 개선할 여지를 찾으러 갔고 **없다는 답**을
가져왔다. 근거 없이 문구를 바꿔 R5에 들고 가는 것보다 `pv1`을 그대로 이관하는 편이 리스크가 낮다.
→ **남은 품질 개선 수단은 프롬프트가 아니라 ⓐ 입력 스펙(§7.1/§7.2)과 ⓑ R6 best-of-N 물량뿐이다.**
→ **미검증 잔여는 그대로다**: I/O 10s는 여전히 **가정**이며 R5 배포 E2E 실측 의무(§A-2)는 해제되지 않았다.

#### ⑧ [신규 결함] cross 스트랩 **재질 위조** — `r3g` 성공작 **10/10 전부**

`r3g`의 ok 10장을 **전수 육안 확인**했다. **10장 전부**에서 스트랩이 **매끈한 민가죽 밴드**로
그려졌다. 실물은 **직조 웨빙 스트랩**(가죽 탭 + 골드 하드웨어)이다.
`pv1`/`pv1d` · `s_casual`/`s_smart`를 가리지 않고 **예외가 없다.**

**원인은 `back` 결함(§A-9 ②)과 구조가 같다 — 레퍼런스에 스트랩이 안 보인다:**

| 채널 | 이 SKU(MMRGATA04CO001)에서 스트랩 정보 |
|---|---|
| ref 1 (정면 팩샷) | 웨빙이 **프레임 좌우 가장자리에서 잘려** 일부만 보인다 |
| ref 2 (보조컷 · 탑다운 578×842) | 스트랩이 **아예 없다** |
| DB `material` 원문 | *"비세토스 모노그램 캔버스, 나파 가죽 트림, 코튼 트윌 안감"* — **웨빙 언급 없음** |

**두 채널이 모두 침묵하면 모델은 지어낸다.** `back`에서는 **색**(검정 → 임의색)을,
`cross`에서는 **재질**(웨빙 → 민가죽)을 지어냈다. **같은 원인, 다른 발현이다.**

→ **"보조컷 1장"이라는 규칙으로는 부족하다. 보조컷이 스트랩을 보여주지 않으면 아무 소용이 없고,
   이 SKU의 보조컷이 정확히 그 경우다.**

**조치 — 00-common §7.1 일반화 개정 (같은 커밋)**: `back` 한정이던 규칙을
**"스트랩 카테고리(`cross`·`shoulder`·`back`)는 스트랩이 선명하게 보이는 컷 1장 필수"**로 넓혔다.
`ref_image_urls`가 계약 필드(00 §5)이므로 **B의 수집 작업에 직접 영향**한다 → B 통지 (d).

> **⚠ 이 결함은 지금까지 어떤 채점에도 잡히지 않았다.** `r3g`에는 `scores.json`이 없고,
> 그 이전 채점들은 c3(스트랩의 **기하**)만 보고 있었다. 재질은 **c1(제품 정합성)** 사안인데
> §4 c1의 fail 기준은 *패턴 왜곡 · 로고 문자 뭉개짐 · 골드/실버 뒤바뀜 · 컬러 이탈* 넷만 열거하고
> **재질 대체를 열거하지 않는다.** 즉 §A-7 ②의 "판정 불능"이 c1에서 반복되고 있었다.
>
> **후속 제안 (이번 커밋에서 미실행)**: §4 c1 fail 기준에
> *"레퍼런스와 다른 재질로 대체(웨빙→가죽, 캔버스→나일론 등)"*를 추가.
> 실행하지 않은 이유는 §4 개정이 **이전 단계와의 비교 가능성을 끊기 때문**이며(§A-7 ⑦ 전례),
> 남은 채점이 R6 선별 하나뿐이라 개정 이득보다 감사 비용이 크다고 판단했다.
> **대신 R6 선별에서 이 항목을 육안으로 반드시 확인한다** — 간판 페르소나 p1의 SKU가 바로 이것이다.

#### ⑨ 이 절이 바꾼 것 (요약)

1. **산출물 ④ 동결** — pv2=pv1 · `gpt_base` · N=2. **R5 이관 스펙이 확정됐다**(⑦).
2. **R3 종료** — 축⑥ 종결(⑤) · 축⑤ 폐기(⑥) · 축②③④ 미착수 종료. R3에 더 쓸 시간이 없다.
3. **폴백 상시화** — 60초 계약은 **조건부**다. §7 리스크 레지스터에 행 신설, B 통지 (a)(b).
4. **§7.1 2차 개정** — 스트랩 카테고리 일반화(⑧). B 통지 (d).
5. **남은 A 작업은 R6 하나다** — `gpt_base` · N8 · `timeout_s: 120` · p1은 **팔짱/크롭 자세**로
   촬영(§A-10 ⑤ — 팔–몸통 틈이 없는 자세에서 cross는 8/8이다).

## 부록 B. 00-common §7 기입 초안 템플릿 (R2 채점 노트로 값 확정)

**§7.1 상품 레퍼런스 이미지 스펙 (초안)**

| 항목 | 초안값 |
|---|---|
| 배경 | 화이트/뉴트럴 팩샷 |
| 필요 각도/장수 | 정면 1장 필수 + 3/4 각도 1장 권장, 상한 3장 |
| 최소 해상도 | 긴 변 1024px 이상 |
| 금기 | 모델 착용컷 / 워터마크 / 로고 가림 / 배경 소품 |

**§7.2 사용자 사진 스펙 (초안)**

| 항목 | 초안값 |
|---|---|
| 프레이밍 | 무릎 위 이상 (back 상품 매칭 대비 반측면 허용 문구는 R4 결과로) |
| 인원 | 1명 |
| 최소 해상도 | 긴 변 1024px 이상 |
| 조명 권고/금기 | 균일광 권장 / 강한 역광·극단 각도·얼굴 가림 금기 |
