# Dev A — 생성 엔진 (Generation Engine)

## 0. 미션

**"사람 사진 + 확정된 상품 이미지 → MCM 화보" 변환, 이 한 가지 문제만 판다.**

성공 기준 우선순위:

1. **제품 정합성** — 진짜 그 가방으로 보이는가 (패턴·로고·하드웨어·비율)
2. 인물 보존 — 얼굴·체형·의상이 원본과 동일한가
3. 합성 자연스러움 — 조명·그림자·착용 위치가 물리적으로 타당한가
4. 화보 감성
5. 속도 (마지막. 로딩 연출은 B가 커버)

가장 경계할 실패 모드: **모델이 '그럴듯한 가짜 가방'을 그려버리는 것.** 비세토스 패턴 왜곡, 금장 로고 뭉개짐, 사이즈 오류 = 럭셔리 브랜드 데모에서 즉사. 모든 판단은 이 기준으로.

v1 정책 (공통 §2): 원본 사진의 배경·인물 유지, 가방 합성 + 톤 보정까지만. 배경 연출은 P2.

---

## 1. [P0] Mock 서비스 최우선 배포

실제 엔진과 무관하게, 공통 문서 §5 계약 그대로 동작하는 mock을 **모든 작업 전에** 만들어 배포한다.

- 내부: 5초 sleep 후 미리 준비한 고정 샘플 이미지 URL 반환. `/health` 포함.
- Railway 배포 → `GEN_BASE_URL` + `GEN_API_KEY`를 B에게 전달.
- **이게 끝나야 B가 병렬로 달릴 수 있다.** A의 최우선 산출물은 엔진이 아니라 이 mock이다.

---

## 2. [P0] 모델 베이크오프

### 사전 준비

- API 키 확보를 먼저 처리 (Google AI, OpenAI, fal.ai 등 — 결제 세팅이 필요한 곳이 있음).
- B로부터 샘플 상품컷 3종 수령 (비세토스 패턴 1, 무지 가죽 1, 나일론 1 — B 문서 §2에 요청되어 있음).
- 실제 셀카 3장 준비 (팀원 사진, 프레이밍·조명 조건을 다양하게).

### 후보 (전부 API 호출형 — 셀프호스팅 금지)

| 후보 | 접근 경로 | 기대 강점 |
|---|---|---|
| Gemini 이미지 편집 계열 (Nano Banana) | Google AI API | 다중 이미지 융합("이 사람에게 이 가방을") + 인물 보존이 강한 것으로 알려짐 |
| FLUX Kontext 계열 | fal.ai / Replicate / BFL API | 지시 기반 편집, 레퍼런스 충실도 |
| gpt-image 계열 | OpenAI API | 다중 입력 합성, 지시 이해 |
| (보조) Seedream/SeedEdit 계열 | fal.ai / Replicate | 대안 확보용 |
| (참고) 전용 Virtual Try-On API (FASHN, Kling 등) | 각 사 | 의류(garment) 특화라 '가방 합성'엔 부적합할 가능성 높음 — 1회만 확인하고 배제 판단 |

기획안의 SD + ControlNet 셀프호스팅은 **하지 않는다.** GPU 인프라·마스크·포즈 추출로 이어지는 토끼굴이며 품질 보장도 없다. 최신 모델 동향은 AI 툴로 짧게 리서치해서 후보를 갱신해도 좋다 — 단, 후보는 최대 4개까지만. 리서치가 실험을 대체하면 안 된다.

### 프로토콜

동일 입력 세트(셀카 3 × 상품 3 = 9조합)를 모델별 동일 조건으로 생성하고, 아래 체크리스트로 pass/fail 판정한다.

- [ ] 패턴·로고·하드웨어가 레퍼런스 상품과 일치
- [ ] 가방 크기·착용 비율이 물리적으로 타당
- [ ] 얼굴·손 왜곡 없음, 인물 동일성 유지
- [ ] 조명·그림자가 원본 사진과 융합
- [ ] 원본 배경 유지 (v1 정책)
- [ ] 전반적 화보 톤

### 산출

1. 승자 모델 1 + 백업 모델 1
2. **Go / No-Go 분기 판단:**
   - **GO** (라이브 생성 품질 확보) → §4~6 진행. 데모 자산(§7)은 어차피 병행.
   - **NO-GO** (품질 불안정) → 라이브 생성은 '되면 좋은 것'으로 격하하고, 데모는 사전 생성 자산 중심으로 확정. **B를 이 문제에 끌어들이지 않는다. 스코프를 줄이는 방향으로만 움직인다.**

---

## 3. [P0] 입력 스펙 2종 확정 — **B 블로킹, 베이크오프 직후 즉시**

베이크오프에서 알게 된 "어떤 입력이어야 품질이 나오는가"를 공통 문서 §7 표에 기입한다.

1. **상품 레퍼런스 이미지 스펙** — 배경/각도/장수/최소 해상도. → 이게 확정돼야 B가 30~50개 대량 수집을 시작한다.
2. **사용자 사진 스펙** — 프레이밍/인원/해상도/조명 금기. → B가 업로드 화면의 가이드 오버레이 + 클라이언트 검증으로 강제한다.

이 두 개가 늦어지면 B의 노동이 통째로 버려지거나 데모날 아무 셀카나 들어온다. **베이크오프 다음으로 급한 산출물.**

---

## 4. [P0] 프롬프트 시스템

- 템플릿: 상품 메타데이터(`name/pattern/material/category`) + `style_hints` → 프롬프트 자동 조립. city → 무드 언어 매핑은 공통 §8 우측 열 기준.
- 핵심 지시 원칙: "레퍼런스 이미지의 제품을 **정확히 그대로**" 계열 강제 문구, 인물·배경 보존 명시, 부정 지시(로고 변형 금지, 신체 변형 금지 등).
- `style_hints`는 선택적 힌트다. 품질을 해치면 무시한다 (계약상 허용됨).
- 재시도 전략: 60초 예산 내에서 — 내부적으로 N=2 생성 후 선택, 또는 실패 시 프롬프트 변형 1회 재시도.
- **모든 생성을 로깅한다** (입력 URL, 프롬프트, 출력 URL, 소요시간). 품질 튜닝과 데모 자산 선별의 원료.

---

## 5. [P0] 서비스 구현 (FastAPI)

- 공통 §5 계약 엄수: `POST /generate` (202 + job_id), `GET /generate/{id}`, `GET /health`, `X-API-Key` 검증.
- Job 저장: 인메모리 dict로 충분. `BackgroundTasks`로 비동기 처리.
- upstream 이미지 API에 자체 타임아웃 → **60초 내 반드시 done/failed 종결.** 영원한 processing 금지.
- 결과 이미지는 `assets/generated/{job_id}.jpg`로 Supabase 업로드 후 공개 URL 반환.
- 에러는 `BAD_INPUT / UPSTREAM_ERROR / TIMEOUT / UNKNOWN`으로 분류해 반환.

---

## 6. [P0] 배포 / 운영

- Railway (또는 동급 컨테이너 호스팅, 콜드슬립 없는 설정).
- env: 이미지 생성 API 키, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `API_KEY`.
- mock → 실엔진 교체 시에도 `GEN_BASE_URL` 불변 유지.

---

## 7. [P0] 데모 자산 사전 생성 (발표 보험)

- 페르소나 6종(공통 §9 표) 사진 확보: **팀원/지인 실사 권장** (스펙 §3 준수), 부족분은 이미지 생성으로 페르소나 제작. 사진 선정은 B와 함께.
- 페르소나당 후보 4~8장 생성 → §2 체크리스트 **전 항목 통과분만** 최고 1장 선별.
- 납품: `apps/web/public/demo/{persona_id}/photo.jpg`, `result.jpg`, `meta.json` (태그, 상품 id, 추천 이유 한 줄).
- GO/NO-GO와 무관하게 무조건 수행 — 라이브 생성이 되더라도 이건 폴백이자 리허설 자산이다.

---

## 8. [P1] 품질 자동화 (여유 시)

- VLM judge: 생성 결과를 VLM에 넣어 "레퍼런스 제품과 동일 제품인가" 자동 판정 → fail 시 자동 재생성.
- N-best 생성 + 자동 선별 파이프라인화.

## 9. [P2] 스트레치

- 도시 배경 연출 모드 (원본 배경 → 도시 무드 변환)
- 멀티 앵글 화보, 생성 속도 단축

---

## Non-goals

VLM 고객 분석, 상품 매칭, 상품 DB, UI, 예약 — 전부 B 소유. 계약(공통 §5) 밖에서 B 코드와 결합하지 않는다.
