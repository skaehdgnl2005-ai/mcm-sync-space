# Dev B — 프로덕트 전체 (Web / Data / Orchestration)

## 0. 미션

사용자가 보고 만지는 전부 + 데이터 + 흐름 제어를 소유한다. 두 관점을 끝까지 유지:

1. **마법이 '보이게'** — 로딩 연출, 화보 리빌, 추천 이유 문장. 기술이 아니라 경험이 심사받는다.
2. **발표가 '절대 안 죽게'** — 데모 모드, 폴백, 무자비한 스코프 컷.

생성 엔진 내부(모델·프롬프트·품질)에는 관여하지 않는다. 계약(공통 §5)과 폴백만 책임진다.

---

## 1. [P0] 인프라 / 레포 셋업 (가장 먼저)

- 모노레포 생성 (공통 §10 구조), Vercel 프로젝트 연결.
- Supabase 프로젝트 생성: Postgres + `assets` 버킷(public read) → **서비스 키를 A에게 즉시 공유** (A의 결과 업로드에 필요).
- Next.js App Router + TS + Tailwind + shadcn/ui 스캐폴딩. **모바일 웹(375~430px) 기준 우선.**
- API 라우트 `maxDuration` 상향 설정 (Vercel 타임아웃 회피). 오케스트레이션은 §6처럼 단계별 라우트로 분리.

---

## 2. [P0] 상품 데이터 (30~50 SKU)

### 소스 확보 방안 (권장 순서)

1. **MCM 공식 온라인 스토어 상품 상세 페이지** (kr.mcmworldwide.com) — 뉴트럴 배경 팩샷 다각도 + 정확한 메타데이터(라인·소재·사이즈·가격)가 한 페이지에 전부 있음. 이 규모에선 **스크래퍼를 만들지 말고 수작업 큐레이션**이 더 빠르고 안전하다 (사이트 구조 변경·봇 차단 리스크 없음).
2. 백업: MCM 글로벌 리전 스토어(이미지 해상도 상이할 수 있음), 공식 입점몰(SSF 등).
3. 용도는 해커톤 데모(비상업, 내부 시연) — MCM을 주제로 한 대회 맥락에서 브랜드 공식 이미지 사용은 자연스럽다.

### 순서 (중요 — 노동 낭비 방지)

1. **먼저 샘플 3종만** 수집 (비세토스 패턴 1, 무지 가죽 1, 나일론 1) → A의 베이크오프에 즉시 전달.
2. A가 상품컷 스펙(공통 §7.1) 확정한 **후에** 대량 수집 시작. 스펙 확정 전 대량 수집 금지.

### 수집 방식

Google Sheet(품번/이름/카테고리/라인/패턴/소재/성별/컬러그룹/가격/이미지 URL/step1·step2 태그) → 시드 스크립트로 DB + Storage 일괄 적재. 메타데이터 구조화는 AI 툴에 상품 페이지 텍스트를 던져 채우면 빠르다.

### 커버리지 매트릭스

STEP1(9) × STEP2(7) 어떤 조합을 골라도 최소 1개 상품이 걸려야 한다. 시트 피벗 또는 체크 스크립트로 검증하고, 빈 조합은 매칭 폴백(§5)이 흡수하는지 확인.

### 스키마 (초안)

```sql
products(
  id text pk, name, category, line, pattern, material,
  gender, color_group, size, price,
  image_urls text[], step1_tags text[], step2_tags text[]
)
reservations(
  id uuid pk, session_id, product_id, generated_image_url,
  persona_summary jsonb,   -- 태그 + VLM 요약 + 매칭 이유
  store, slot, created_at
)
```

---

## 3. [P0] 모바일 웹 플로우

럭셔리 톤 유지 — 블랙/코냑 팔레트, 넉넉한 여백, 세리프 헤드라인, 과한 그라데이션 금지.

| # | 화면 | 핵심 요구사항 |
|---|---|---|
| 1 | 랜딩 | 브랜드 톤 인트로 + 시작 CTA |
| 2 | 사진 업로드 | **A의 사용자 사진 스펙(공통 §7.2) 반영** — 실루엣 가이드 오버레이, 클라이언트 검증(해상도·비율·1인), 촬영/앨범 지원, 업로드 즉시 Storage 저장 |
| 3 | STEP1 도시 태그 | 9개 (공통 §8 slug), 1개 선택 |
| 4 | STEP2 TPO 태그 | 7개, 1개 선택 |
| 5 | **로딩 시어터** | 실제 파이프라인 단계에 연동한 카피 전환: "스타일 분석 중 → 매장 컬렉션 매칭 중 → 화보 렌더링 중". 15~60초 체감 커버가 존재 이유. 프로그레스는 fake+실제 혼합 |
| 6 | 결과 (리빌) | 화보 풀스크린 리빌 연출 + 상품 카드(이름/라인/소재/가격) + **매칭 이유 한 문장** + "이 가방, 매장에 준비해두겠습니다" CTA |
| 7 | 예약 | 매장(1곳 고정 가능) + 시간 슬롯 → 원클릭 확정 → 확인 화면 |
| 8 | 매니저 뷰 `/manager` | 태블릿 가로 기준. 예약 리스트(시간/페르소나 요약/매칭 상품/화보 썸네일), 5초 폴링. 인증 없이 비공개 URL |

---

## 4. [P0] VLM 고객 분석 (내부 전용)

- 모델: Gemini Flash 계열 또는 GPT-4o 계열, **structured output(JSON) 필수.**
- 출력 스키마: `{ color_palette, tone(warm|cool|mono|pastel|vivid), fit_style, gender, lighting(natural|warm|night) }`
- `lighting`만 `style_hints`로 A에게 전달, 나머지는 매칭 전용. **이 스키마를 계약에 노출하지 않는다** (공통 §3).

---

## 5. [P0] 매칭 엔진

2단 구조 (Pinecone 없음 — 공통 §2):

1. **규칙 필터**: STEP2 → category 풀, STEP1 → line/pattern 풀 (초기 규칙은 공통 §8, 카탈로그 확보 후 조정), VLM의 gender/tone으로 축소.
2. **LLM 최종 선택**: 후보 상품 메타데이터 + 고객 분석 JSON → 1개 선택 + **선택 이유 한 문장 생성** (결과 화면과 매니저 뷰에 노출 — 심사위원 설득력의 핵심).

- 폴백: 후보 0개 시 필터를 pattern → line → category 순으로 단계적 해제. 절대 빈 결과를 내지 않는다.
- 매칭 로그 저장 (선택 이유 포함).

---

## 6. [P0] 오케스트레이션 API

단계별 라우트로 분리 (각 호출을 짧게 유지, Vercel 타임아웃 회피):

| 라우트 | 역할 |
|---|---|
| `POST /api/session/upload` | Storage 저장 → `photo_url` 반환 |
| `POST /api/analyze` | VLM + 매칭 → `{ product, style_hints, rationale }` 반환 |
| `POST /api/generate/start` | A의 `POST /generate` 프록시 → `job_id` 반환 |
| `GET /api/generate/status` | A의 `GET /generate/{id}` 프록시 (클라이언트가 2초 폴링) |

실패 처리: 60초 초과 또는 `failed` → 자동 재시도 1회(신규 start) → 그래도 실패 시 **데모 모드 자산으로 폴백** + 자연스러운 카피 ("컨시어지가 준비한 스타일링을 먼저 보여드릴게요" 류). 에러 화면을 심사위원에게 보여주지 않는 것이 원칙.

---

## 7. [P0] 데모 모드

- 진입: `?demo=1` → 랜딩에 페르소나 6종 선택 UI (발표자용, 숨김 스타일).
- 선택한 페르소나의 사진·태그·매칭·화보를 전부 `public/demo/{persona_id}/*` 정적 자산에서 로드. **외부 API 0회 호출**로 전체 플로우 재생. 로딩 연출은 동일하되 12~15초로 단축.
- 자산은 A가 납품 (공통 §9) → B가 커밋·연결. 페르소나 사진 선정은 A와 함께.
- 일반 모드 실패 폴백 경로(§6)와 코드 공유.
- 발표용 고정 URL + QR 이미지 준비 (발표자에게 전달).

---

## 8. [P0] 예약 + 매니저 뷰

§3의 7·8 구현. 푸시 없이 폴링. 예약 생성 시 `persona_summary`(태그 + VLM 요약 + 매칭 이유)를 저장해 매니저 뷰에 그대로 노출 — "매장이 고객을 미리 안다"는 B2B 가치를 화면 하나로 증명하는 장치다.

---

## 9. [P1] 폴리싱 (여유 시)

- 화보 저장/공유 (다운로드, 카톡 공유 OG 메타태그)
- "다른 스타일 보기" — 차순위 상품으로 재생성하는 캐러셀
- 마이크로 인터랙션(리빌 애니메이션 강화), 매니저 뷰 상세 모달

---

## Non-goals

이미지 생성 엔진 내부(모델 선택·프롬프트·품질 튜닝) — A 소유. 생성이 막혀도 함께 뛰어들지 않는다. B의 대응은 폴백과 스코프 컷뿐이다.
