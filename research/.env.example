# research/.env 로 복사해 채운다. .env는 커밋되지 않는다 (루트 .gitignore).
# 시크릿은 env만 — 설정 파일·코드에 키를 적지 않는다 (06 §1 N6).
#
# B1~B2(fake E2E)는 키 없이 돈다. 아래는 B4(실프로바이더)부터 필요하다.

GOOGLE_API_KEY=
OPENAI_API_KEY=

# 컨틴전시 — R1 전멸(G1) 시에만 발급 (05 §3)
# FAL_KEY=
