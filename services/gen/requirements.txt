# 03-mock-service-spec.md §1 — 의존을 최소로 유지한다.
# R5(실엔진 이관)에서 아래 "실엔진" 블록이 추가됐다. 그 외 추가 금지.
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4

# --- 실엔진 (R5) -------------------------------------------------------------
# httpx: 입력 이미지 다운로드 + Supabase Storage REST 업로드.
#        supabase-py를 쓰지 않는다 (06 §11-2) — 같은 httpx를 재사용해 의존 1개를 아낀다.
httpx==0.28.1

# 03 §1은 "SDK 1개"라고 적었지만 그건 승자가 **하나**이던 시절의 문장이다.
# 05 부록 A-16 ④ · A-17 ①이 경로별로 벤더를 갈랐고(라이브 gemini / 사전생성·폴백 gpt),
# 폴백의 가치는 **"발표장 회선이 나쁘면 env 한 줄로 전환"**에 있다. SDK가 이미지에 없으면
# 그 전환은 재배포를 부르고, 그러면 폴백 카드가 카드가 아니게 된다. 그래서 둘 다 넣는다.
# 보유 비용은 빌드 용량뿐이고(런타임 import는 ENGINE 값에 따라 lazy), 얻는 것은 롤백 경로다.
google-genai==2.8.0    # ENGINE=gemini_nb (라이브 기본값)
openai==3.1.0          # ENGINE=gpt_image (폴백)
