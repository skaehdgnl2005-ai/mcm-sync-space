# MCM Sync-Space — 프로젝트 컨텍스트 (AI 세션용) (v2)

> \*\*이 문서를 읽는 AI에게\*\*: 이것은 해커톤 프로젝트의 맥락 요약이다. 코드를 쓰기 전에 §6 불변 결정사항을 지켜라. 요청받은 작업이 이 문서나 §7의 상세 문서와 충돌하면, 임의로 진행하지 말고 충돌을 지적하라. 요청받지 않은 스코프 확장(기능 추가, 라이브러리 추가, 계약 필드 추가)을 하지 마라. 상세 스펙은 §7 문서 맵의 해당 문서가 우선한다.

> \*\*v2 개정 이력\*\*: 팀원 제공 제품 데이터 반영 — 고객 태그 2단계→\*\*3단계\*\*(도시 4종·목적 4종·컬러 4종, 도시 무드 재정의), VLM 7축 도입, 계약에 `wear\_position` 추가, 의류는 v1 매칭·생성 제외, 상품 데이터는 팀원 공급 파이프라인으로 전환.

\---

## 1\. 무엇을 만드는가

**MCM Sync-Space** — 해커톤 무대 발표용 데모. 고객이 데일리룩 셀카 1장을 올리고 감성 태그 3개(도시 무드 → 목적·형태 → 컬러 포인트)만 고르면, AI가 어울리는 MCM 제품을 매칭해 **고객 본인의 사진 위에 화보급 시착 이미지를 생성**하고, 확신이 가장 높아진 그 순간에 **오프라인 플래그십 매장 프라이빗 피팅 예약**으로 연결하는 O2O 서비스.

## 2\. 왜 만드는가

럭셔리 이커머스는 "이게 내 평소 스타일과 어울릴까"라는 시각적 불확실성 때문에 구매 전환율이 약 1.1%에 그치고, 오프라인 플래그십 매장은 트래픽 가뭄에 시달린다. 생성 화보로 온라인에서 시각적 확신을 만든 뒤, 전환율이 훨씬 높은 오프라인 방문으로 고객을 보내는 다리를 놓는 것이 핵심 가설이다. 럭셔리 브랜드(MCM)가 무대이므로 **톤앤매너(UI)와 제품 재현 정확도(생성)가 곧 신뢰**다.

## 3\. 데모 골든 패스 (심사위원이 보게 될 것)

모바일 웹 접속 → 전신 셀카 업로드 → STEP1 도시/무드 1개(#밀라노·#도쿄·#파리·#뮌헨) → STEP2 목적·형태 1개(#출근·미팅 / #데일리·산책 / #활동성·이동 / #외출·파티) → STEP3 컬러 포인트 1개(#모던\_무채색 / #아이코닉\_꼬냑 / #소프트\_뉴트럴 / #비비드\_컬러팝) → 로딩 연출 → AI 매칭 상품 + 생성 화보 리빌 + 추천 이유 → 원클릭 매장 예약 → 매니저 태블릿에 고객 취향·매칭 SKU·화보 표시.

무대 발표이므로 **"절대 죽지 않는 데모"가 최우선 가치**다. 외부 API 0회 호출로 전체 플로우를 재생하는 **데모 모드**(페르소나 6종, 이 중 3종은 밀라노 실물 SKU로 확정)가 정식 P0 기능이며, 일반 모드 실패 시 폴백 경로로도 쓰인다.

## 4\. 아키텍처와 역할 분담

```
\[팀원(비개발)] ──에디션별 제품 데이터(xlsx 템플릿)──> \[Dev B 시드 스크립트] ──> Supabase
\[모바일 웹  Next.js/Vercel — Dev B]
   │ (전부 server-side API Routes)
   ├──> Supabase (Postgres + Storage)
   ├──> VLM API (고객 사진 분석 — B 내부 전용)
   └──> \[Gen Service  FastAPI/Railway — Dev A] ──> 외부 이미지 생성 API
\[매니저 태블릿] ── 폴링 ──> 웹의 /manager
```

* **Dev A**: 생성 엔진 한 가지만 — **(사람 사진 + 확정된 상품 이미지 + 착용 위치) → 화보** 변환.
* **Dev B**: 그 외 전부 — 웹 UI, 상품 DB(에디션별 12 SKU × 4도시 목표), VLM 분석, 매칭, 오케스트레이션, 예약/매니저 뷰, 데모 모드.
* 호출은 **B→A 단방향**, 비동기 job API(`POST /generate` → 202+job\_id → 폴링). 계약 전문은 `00-common.md` §5 (v2).

## 5\. AI 파이프라인 (내부 로직 요약)

1. **VLM 분석** — 고객 사진에서 7축(포멀도·실루엣·소재감·컬러 조화·패턴·메탈 톤·합성 가능 위치 판단은 제품측) + 성별·조명을 구조화 추출 (B 내부 전용, 계약 비노출)
2. **매칭** — 하드 필터(도시=에디션, 목적, matchable, 성별) → 컬러 포인트 필터 → **고객 착장 태그 ∩ 제품 '적합 착장 태그' 오버랩 스코어링**(메탈 톤 일치 부스트) → LLM 최종 선택 + 추천 이유 한 문장 (벡터DB 사용 안 함)
3. **생성** — 매칭 상품의 레퍼런스 이미지를 고객 원본 사진의 \*\*지정 착용 위치(wear\_position)\*\*에 융합. **v1은 원본 배경·인물 유지, 상품 합성 + 톤 보정까지만** (배경 연출은 스트레치)

## 6\. 불변 결정사항 (재논의·위반 금지)

* 이미지는 서비스 간 **항상 URL로 전달**, base64 금지
* 생성 job은 **60초 내 done/failed 종결**. B는 2초 폴링·최대 60초 대기, 실패 시 1회 재시도 후 데모 자산 폴백
* 품질 우선순위: **제품 정합성(패턴·로고·하드웨어 톤) > 인물 보존 > 합성 자연스러움(지정 위치·조명) > 화보 감성 > 속도** — 속도는 로딩 연출로 커버
* 생성은 **API 호출형 모델만** (gpt-image-2, Nano Banana 계열 등 — 베이크오프로 확정). SD+ControlNet 셀프호스팅 금지
* **의류(garment) 카테고리는 v1 매칭·생성 제외** (DB 보관, matchable=false). 스카프(neck)는 베이크오프 검증 후 확정
* 태그 slug는 `00-common.md` §8 v2가 유일 기준: 도시 4종(**실제 에디션 축 — 밀라노=스트리트/트렌디, 도쿄=비즈니스/미니멀 등, 구버전 무드 정의와 반대이므로 주의**)·목적 4종·컬러 4종·VLM 7축+성별·조명
* 계약 v2: `product.id`=MCM SKU, `wear\_position` 필수, `material`/`color\_hardware`는 DB 원문 텍스트, `style\_hints`는 city/purpose/lighting
* 스마트 미러·실시간 푸시·로그인 없음 (슬라이드 목업·폴링·localStorage 세션ID로 대체)
* 모노레포, **main 브랜치는 항상 데모 가능 상태 유지**
* 인터페이스 계약 변경은 두 개발자 합의 + `00-common.md` 동시 갱신 없이 금지

## 7\. 문서·데이터 맵

|파일|내용|
|-|-|
|`00-common.md` (v2)|단일 소스: 계약, 스토리지, 태그 사전, 데모 모드, 레포/환경변수, 완성 기준, 미결 사항|
|`01-dev-a-generation.md` (v2)|Dev A: 베이크오프(실 SKU 3종), 입력 스펙, 프롬프트, 서비스, 데모 자산|
|`02-dev-b-product.md` (v2)|Dev B: 웹 플로우(태그 3화면), 데이터 파이프라인, VLM, 매칭, 오케스트레이션|
|`MCM\_제품데이터\_정규화\_및\_수집템플릿.xlsx`|밀라노 12 SKU 정규화(DB 시드 소스) + 잔여 도시 수집 템플릿|



