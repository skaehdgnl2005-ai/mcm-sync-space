# MCM Sync-Space — 프로젝트 컨텍스트 (AI 세션용)

> **이 문서를 읽는 AI에게**: 이것은 해커톤 프로젝트의 맥락 요약이다. 코드를 쓰기 전에 §6 불변 결정사항을 지켜라. 요청받은 작업이 이 문서나 §7의 상세 문서와 충돌하면, 임의로 진행하지 말고 충돌을 지적하라. 요청받지 않은 스코프 확장(기능 추가, 라이브러리 추가, 계약 필드 추가)을 하지 마라. 상세 스펙은 §7 문서 맵의 해당 문서가 우선한다.

---

## 1. 무엇을 만드는가

**MCM Sync-Space** — 해커톤 무대 발표용 데모. 고객이 데일리룩 셀카 1장을 올리고 감성 태그 2개만 고르면, AI가 어울리는 MCM 가방을 매칭해 **고객 본인의 사진 위에 화보급 시착 이미지를 생성**하고, 확신이 가장 높아진 그 순간에 **오프라인 플래그십 매장 프라이빗 피팅 예약**으로 연결하는 O2O 서비스.

## 2. 왜 만드는가

럭셔리 이커머스는 "이게 내 평소 스타일과 어울릴까"라는 시각적 불확실성 때문에 구매 전환율이 약 1.1%에 그치고, 반대로 오프라인 플래그십 매장은 트래픽 가뭄에 시달린다. 생성 화보로 온라인에서 시각적 확신을 만든 뒤, 전환율이 훨씬 높은 오프라인 방문으로 고객을 보내는 다리를 놓는 것이 핵심 가설이다. 럭셔리 브랜드(MCM)가 무대이므로 **톤앤매너(UI)와 제품 재현 정확도(생성)가 곧 신뢰**다.

## 3. 데모 골든 패스 (심사위원이 보게 될 것)

모바일 웹 접속 → 전신 셀카 업로드 → STEP1 도시/무드 태그 1개 선택(#밀라노 등 9종) → STEP2 TPO 태그 1개 선택(#비즈니스 미팅 등 7종) → 로딩 연출 → AI 매칭 상품 + 생성 화보 리빌 → 원클릭 매장 예약 → 매니저 태블릿에 고객 취향·매칭 상품·화보가 표시됨.

무대 발표이므로 **"절대 죽지 않는 데모"가 최우선 가치**다. 외부 API 0회 호출로 전체 플로우를 재생하는 **데모 모드**(사전 준비된 페르소나 6종)가 정식 P0 기능이며, 일반 모드 실패 시 폴백 경로로도 쓰인다.

## 4. 아키텍처와 역할 분담

```
[모바일 웹  Next.js/Vercel — Dev B]
   │ (전부 server-side API Routes)
   ├──> Supabase (Postgres + Storage)
   ├──> VLM API (고객 사진 분석 — B 내부 전용)
   └──> [Gen Service  FastAPI/Railway — Dev A] ──> 외부 이미지 생성 API
[매니저 태블릿] ── 폴링 ──> 웹의 /manager
```

- **Dev A**: 생성 엔진 한 가지만 — **(사람 사진 + 확정된 상품 이미지) → 화보** 변환.
- **Dev B**: 그 외 전부 — 웹 UI, 상품 DB(30~50 SKU), VLM 분석, 매칭, 오케스트레이션, 예약/매니저 뷰, 데모 모드.
- 호출은 **B→A 단방향**, 비동기 job API(`POST /generate` → 202+job_id → 폴링). 계약 전문은 `00-common.md` §5.

## 5. AI 파이프라인 (내부 로직 요약)

1. **VLM 분석** — 고객 사진에서 컬러 팔레트·톤·핏·성별·조명을 구조화 추출 (B 내부 전용, 계약에 노출 안 함)
2. **매칭** — 태그 규칙 필터(STEP2→가방 카테고리, STEP1→라인/패턴) → LLM이 최종 1개 선택 + 추천 이유 문장 생성 (벡터DB 사용 안 함)
3. **생성** — 매칭된 상품의 레퍼런스 이미지를 고객 원본 사진에 융합. **v1은 원본 배경·인물 유지, 가방 합성 + 톤 보정까지만** (배경 연출은 스트레치)

## 6. 불변 결정사항 (재논의·위반 금지)

- 이미지는 서비스 간 **항상 URL로 전달**, base64 금지
- 생성 job은 **60초 내 done/failed 종결**. B는 2초 폴링·최대 60초 대기, 실패 시 1회 재시도 후 데모 자산 폴백
- 품질 우선순위: **제품 정합성(진짜 그 가방으로 보임) > 인물 보존 > 합성 자연스러움 > 화보 감성 > 속도** — 속도는 로딩 연출로 커버
- 생성은 **API 호출형 모델만** (gpt-image-2, Nano Banana 계열 등 — 베이크오프로 확정). SD+ControlNet 셀프호스팅 금지
- 스마트 미러·실시간 푸시·로그인 없음 (각각 발표 슬라이드 목업·폴링·localStorage 세션ID로 대체)
- 태그 slug(도시 9종, TPO 7종)는 `00-common.md` §8이 유일 기준이며 불변
- 모노레포, **main 브랜치는 항상 데모 가능 상태 유지**
- 인터페이스 계약 변경은 두 개발자 합의 + `00-common.md` 동시 갱신 없이 금지

## 7. 문서 맵

| 문서 | 내용 |
|---|---|
| `00-common.md` | 단일 소스: 인터페이스 계약, 스토리지 규약, 태그 사전, 데모 모드, 레포/환경변수, 완성 기준 |
| `01-dev-a-generation.md` | Dev A 태스크: 베이크오프, 입력 스펙, 프롬프트 시스템, 서비스, 데모 자산 |
| `02-dev-b-product.md` | Dev B 태스크: 웹 플로우, 상품 데이터, VLM, 매칭, 오케스트레이션, 예약/매니저 뷰 |
| `03-mock-service-spec.md` | Mock 생성 서비스 구현 스펙 (Dev A 첫 산출물) |

## 8. 현재 상태 — *세션 시작 전 직접 갱신할 것*

- 진행 단계: (예: mock 배포 완료 / 베이크오프 진행 중 / 실엔진 교체 중)
- 확정 생성 모델: (미정 — 베이크오프 전)
- `GEN_BASE_URL`: (배포 후 기입)
- 미결 사항: (예: 페르소나 사진 소싱 방식)
